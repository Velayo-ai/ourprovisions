# OurProvisions — Roadmap
*Last updated: 2026-07-18 (+ **designed the household-management redesign and merged it with OurBanner into ONE Phase I build**: entity/membership model — **collection → selected → scoped detail** (households CRUD above the line, this-household membership below, one selection driving both, membership recomputing on switch); the redesign's pencil opens the Edit sheet OurBanner fills, so they are **one build, not two**. Active-row affordance = a **bare pencil** (FINAL3; pill variant built + rejected; "ACTIVE" word dropped); **Delete moves into the Edit sheet** (danger zone, creator-only — refines OurBanner D2); "Created by you" resolves to **creator-gated Delete visibility** (D4); Invite = **`navigator.share()` hand-off** (deletes the persistent-banner defect by construction; "Copy link" removed); rows **name-only** (monogram/anchor dropped); both zones on one clay eyebrow (`YOUR HOUSEHOLDS` / `{HOUSEHOLD} · MEMBERS`). Spec locked (`docs/specs/active/SPEC_household_management_phase1.md`, D1–D8) + mockup of record `docs/mockups/mockup_household_manage_FINAL3.html`. **Fulfills the long-standing "Manage-household surface redesign" NEXT item** (direction set 2026-06-29). **Events scoped OUT to Phase II** (design-first: households ∪ events as one Context entity with optional parent + optional deadline; temporary non-inheriting event membership; photo-as-context-identity generalizing OurBanner — standalone case: a bachelorette at a hotel, no household, photo IS its identity). Migration **024 still authored/not applied** — folded in a one-char verify-block paren fix (`and X or Y` → `and (X or Y)`; `AND` was binding tighter than `OR`). 5 new DECISIONS. **Phase I build = NOW, not built this session** (design only). Prior 2026-07-18 (+ closed a **LIVE PROD DATA EXPOSURE** and designed Beat 1: the `{anon}` SELECT policy on `catalog_items` (named *"Anyone can read global catalog items"*) had **no `is_global` check** — all 183 rows incl. 133 household-custom free-text rows (a beta user's parent's medication schedule) were anon-readable. **Migration 022 applied dev+prod, browser-verified 183→50** (0 custom) → **DONE**. Principle: *a query asks for what it wants; RLS is the backstop, not the filter* (the client fetch also lacked the filter — two layers failed). `catalog_items` is **PII-bearing, not reference data**. New NEXT: **anon-surface audit** (`category_avg_prices` rides the same signed-out path) + **disclosure decision** (Dan's call). Designed **OurBanner** (Beat 1 household photo header) — household-owned photo + framing (drag + **zoom**, mandatory: a 2.6:1 band needs ~165% on a real photo) + 3-state wordmark (`large`/`small`/`hidden`); **the group sets the look** (household state, any member); mockup approved (`docs/mockups/mockup_ourbanner.html`). Scrim superseded: **one principle, two treatments** — radial (landing hero, we control the photo) vs **band** (app header, assumes nothing). Migration **024 authored, not applied** (build next session); **referral renumbers 022→023** (third double-claim in four migrations → *assign migration numbers at point-of-build*). 12 new DECISIONS. Prior 2026-07-17 (+ built & shipped the landing page: **migration 021 `beta_signups` APPLIED dev+prod** (14 cols, RLS armed, one anon INSERT-only policy — the **021 double-claim is RESOLVED**: `beta_signups` took 021, the **referral primitive renumbers to ≥022**). Built **`ourprovisions-landing`** as a standalone static repo (no React/Supabase-client/Clerk), pushed to `Velayo-ai/ourprovisions-landing`; arrival mechanic browser-verified (nothing parsed, fire-and-forget insert opens the door even on failure). **Captions rewritten** so heading and body do different jobs — Shop → *"Get in, get out, get it right"* (sells speed), Browse → *"The stuff you buy every week"*. **Footer** → 3-col grid with `Velayo · Privacy · Terms` (absolute `velayo.ai` URLs — one canonical legal copy for the fleet). Mockup overwritten as the tiebreaker; `color-scheme:light` added. **Remaining before live:** Vercel deploy+env, anon-cannot-read/CORS verify on the deployed URL, Cloudflare grey-cloud DNS. New NEXT: **privacy-policy revision** (draft ready, `velayo-web` scope, §07 blank for counsel — the live policy contradicts the page's *"we read every answer"*). ★ New principle — **judge the live page in the real browser, not a mockup in an in-app viewer** (four rounds chasing a dark-mode bug that never existed). 8 new DECISIONS. Prior 2026-07-16 (+ merged the landing-page design handoff: chose the **TOLL access model** — completing the seven-question questionnaire *grants* beta access rather than requesting it, killing the "Dan sets you up" bottleneck and the "invite only" fiction; the page can no longer say "invite only". **Two-surface architecture**: `ourprovisions.app` (static, public, indexed front door) + `ourprovisions.velayo.ai` (Clerk app) as **separate Vercel projects** — marketing at the app root fights Clerk's redirect logic. Questionnaire cut nine→**lucky seven** on "a question earns its place only if it changes a decision we're actually going to make"; added `shop_mode` (in-store/delivered/mixed — a **roadmap-forking** find: a service-shopper isn't served by Phase 2 aisle-ordering), dropped `store_count`+`list_method`, kept `region` unasked. Mockup **APPROVED** (`docs/mockups/mockup_landing.html`, load-bearing hero values `100% auto` / `center 42%`). **Build + ship the landing page → NOW** (migration 021 `beta_signups`, Vercel project, Cloudflare DNS grey-cloud, anon insert, CORS). New NEXT: **blog home** (where `docs/content/blog_july_beta.md` lives — likely `/blog` on `.app`) + **Mailchimp OUTBOUND welcome email** (not capture). New LATER: **hero video** (boat/water, swaps into the image layer) + **future-state hero** (swap hero device to the household photo header once Beat 1 ships). **Beat 1 (household photo header) gains three hard requirements** — `background-size`+`background-position` not a crop, a **radial** scrim that survives off-centre subjects, black not espresso — plus **EXIF-orientation normalize on import** (proven tonight: Claude mis-cropped 3×, Dan fixed in 5s with a slider — `docs/mockups/crop_tuner.html` kept as the reference prototype). **Retired** `SPEC_beta_signups.md` → `specs/retired/` (never built; superseded by `SPEC_landing_page_beta.md`; table confirmed absent on prod). New folder **`docs/content/`** for written content (blog, email); `household_photo_header.html` finally filed to `docs/mockups/` (closed the "approved mockups must ride the manifest" process gap). ⚠️ **Migration 021 is double-claimed** — both `beta_signups` and the referral primitive want 021; reconcile at build (one takes ≥022). 17 new DECISIONS. Prior 2026-07-16 (+ merged the backup-floor / A-fix-closure / referral-design handoff: took the **first prod backup since March** — TOC-verified off-instance `pg_dump`, `*.dump` gitignored (→ DONE); a floor, not a strategy, so **backup cadence decision** → NEXT. **Contributor A-fix closed** — dev-verified + merged dev→main `abc5f15`, prod-verified (→ DONE). Re-diagnosed the beta blocker: it is **not invite-arrival friction, it is that there is no front door** — `beta_signups` has no sign-up page/questionnaire/welcome email → **"Write the front door" promoted to NOW/P0**. "Invite-arrival friction is systemic" AMENDED to **three failures in three layers** (Aidan=expectation, Heddi=delivery, Chris=app); the 2026-07-14 **RCS rich-card root cause is OVERTURNED** (Chris's carded Android link worked). New NEXT items: **referral primitive** (migration 021 `referral_code`+`referred_by`, Preferences-placed — NOT the household share sheet; per `docs/specs/active/SPEC_referral_primitive.md`) and **query Chris's failed join against prod**. Migration bookkeeping RESOLVED: high-water 020, next 021 (referral); 009–012/017 gaps + dup 007 = honest drift, do NOT reconstruct. Android link-format repro → LATER. 14 new DECISIONS (referral = advocacy + defect-surface fix; no gamification; measure `referred_by` not sends; mission on the page not the button; never reconstruct a migration; a backup isn't a backup until TOC-verified + off-instance). Prior 2026-07-15 (+ merged the prod-outage / alerting handoff: a wedged PostgREST at the Supabase platform layer hung prod on "Loading your provisions…" — restarted the project to restore; diagnosed by killing hypotheses (idle DB = unreached not overwhelmed; 522 Cloudflare→origin uniform across endpoints; 3 of 3,000 IOPS). Stood up **user-visible alerting**: Splunk RUM JS-error detector + Synthetics Data-API uptime detector (exercises PostgREST, not the Vercel shell) + a detector on the pre-existing browser test — all backtested/verified before activation. 6 DECISIONS: don't upgrade compute (~0.1% IO used); alert on user-visible failure not infra metrics; Synthetics must hit the Data API; defer the OTel Collector to Phase 3–4; single Synthetics location; watch Supabase legacy-key deprecation. Follow-ups → NEXT (RUM threshold tuning) + LATER (OTel Collector). Same-day earlier — merged the retroactive 2026-07-14 Chris/Heddi beta-feedback handoff: invite-arrival friction is systemic (3/3 testers failed link→app) → NEXT; referral≠invite primitive; priority signaling on `list_items` (3-state); hidden-items findability = mostly F1b, grouped-in-search residual → NEXT; multi-household member-picker Option B (SECURITY DEFINER RPC trap); event-as-provisioning-context → LATER; 10 new DECISIONS. Same-day: Shared-list integrity arc CLOSED — F0 `uq_live_list_item` live dev+prod + F1b hide→re-add no-stomp built/verified/merged `28539af`; Add-pill drift cleared; contributor-attribution display fix shipped to dev (Shop name-line derives from the ledger, not stale `added_by`) — UNVERIFIED, prod Supabase platform incident; contributor badge MODEL designed + specced, build-gated. Prior 2026-07-13: Shared-list data-integrity Bugs 1/2/3 FIXED + prod-verified — Bug 1 catalog fork killed via migration 018 idempotent RPC + 019 unique indexes + 12-fork-set prod cleanup; Bug 2 row-scoped toggle write; Bug 3 pendingCheckRef optimistic guard; commit `b85dcbf` → main `851b5ae`. Remaining hardening F0/F1b → NEXT. Prior 2026-07-11: docs/ reorg EXECUTED — spec lifecycle folders `docs/specs/{active,built,retired}/` created + ~43 files sorted, commit `0303397`; earlier same day: Declutter cycle shipped + filter-reset fix on dev; beta field-test capture — Helen + Aidan — added Tier-1 shared-list bugs + Household photo header to Beat 1, nav/affordances design to NEXT, invisible-affordances pattern)*

---

## Status Key
- **NOW** — actively building this week
- **NEXT** — clearly defined, ready to start
- **LATER** — planned but not yet spec'd
- **DONE** — shipped and live

---

## NOW — Active Sprint

### Beat 0 — Launch floor + two-way channel *(this week)*

| Priority | Feature | Spec |
|---|---|---|
| **P0 — NOW (build done 2026-07-17; deploy pending)** | **Take the `ourprovisions.app` landing page LIVE.** Build is DONE: **migration 021 `beta_signups` applied+verified dev+prod** (14 cols, RLS armed, one anon INSERT-only policy — the absence of SELECT is the security); **`Velayo-ai/ourprovisions-landing` built + pushed** (standalone static repo, arrival mechanic browser-verified — nothing parsed, fire-and-forget insert opens the door even on failure); captions + footer synced from the mockup. **Remaining to go live:** (1) create the separate Vercel project, set `SUPABASE_URL` + `SUPABASE_ANON_KEY` env vars, deploy to the Vercel URL; (2) verify on the deployed URL — a stranger's submission lands a prod `beta_signups` row, an anon-key `select *` returns **zero rows** (the critical `fit_note`-safe test), and **CORS** allows `ourprovisions.app` (check the console, not just the row count); (3) Cloudflare **grey-cloud** DNS (orange loops TLS). **Done when** `ourprovisions.app` resolves over TLS and the anon insert + zero-row read both pass live. Per `docs/specs/active/SPEC_landing_page_beta.md` + `docs/mockups/mockup_landing.html` (tiebreaker). ⚠️ No `vercel`/`gh` CLI in the Claude Code env — Dan owns deploy + DNS. | `docs/specs/active/SPEC_landing_page_beta.md` |
| **P0 — DONE (design) 2026-07-16** | *(**Write the front door — sign-up page + seven-question questionnaire copy** — DESIGN/COPY DONE 2026-07-16: TOLL access model chosen, questionnaire cut to the lucky seven (`shop_mode` added; `store_count`/`list_method` dropped; `region` kept unasked), thesis rewritten off "the shop is the pain, the list is the instrument", pricing commitment caught + cut, arrival mechanic designed. Superseded by the build item above.)* | (design done) |
| **P0 — PROD-VERIFIED ✓ (→ DONE)** | **False "No longer a member" banner leak** — ROOT-CAUSED + FIXED 2026-07-09, **PROD-VERIFIED 2026-07-10** (no observed defects). Not a stale-storage flag: a deliberate delete/leave and the 30s `checkPresence` watchdog both react to one membership loss; the poll fired the loud notice during the reconciliation gap. Fixed via `deliberateLossRef` raised before the destructive RPC (`081c641`) + name-correctness fix. **Last gate on the signup-email flip — now cleared; the flip is unblocked** (see NEXT). | — |
| Beat 0 | **iOS install coach-mark** — iOS Safari only; first-open show; two-step dismissal ladder (shows twice, then never again); localStorage counter per device. Android deferred to Chrome native prompt. | `docs/SPEC_pwa_install_coachmark.md` |
| Beat 0 | **"Message the bridge" feedback** — In-app feedback channel: auto-captures page, household ID, app version, Clerk ID + user message; store + notify path with store-only fallback. | `docs/SPEC_feedback_bridge.md` |
| Beat 0 | **Dispatches** — In-app what's-new surface: one authored beat message → Mailchimp + in-app banner + social. **Dismissal behavior TBD** (do not build dismissal until chosen). | `docs/SPEC_dispatches.md` |

*Beat 0 shipped this week (→ DONE): Join-activation P0 (reopen prod-verified), auth-gate cold-load fix, `household_staples` (Bug A), cold-start empty-Browse race (Bug B), new-user cold-start walk (result: PARTIAL GREEN — held on personal signup email pending the banner-leak blocker above).*

### Beat 1 — Field-test findings *(from watching Helen + Aidan on prod, 2026-07-11 — prioritized by threat-to-beta-success)*

| Priority | Feature | Spec |
|---|---|---|
| **Tier 1 — SHIPPED ✓ (→ DONE)** | **Shared-list data-integrity bugs (1, 2, 3)** — ROOT-CAUSED + FIXED + **PROD-VERIFIED 2026-07-13**. Bug 1 was a **catalog-layer fork** (prod query overturned the list-layer guess): fixed by migration 018 (idempotent `insert_custom_catalog_item`, normalized-name reuse) + migration 019 (partial unique indexes) + a reversible cleanup of 12 prod fork sets. Bug 2 = row-scoped toggle write (`.eq("id", listItemId)`); Bug 3 = `pendingCheckRef` optimistic guard. F2+F3 in `b85dcbf` → main `851b5ae`. **Remaining hardening (in NEXT):** F0 (`list_items` live-row unique index) + F1b (resolver eviction). See DONE + `docs/specs/active/SPEC_shared_list_integrity.md`. | active spec |
| **Phase I — NOW (designed 2026-07-18; build next session)** | **Household-management redesign + OurBanner — ONE Phase I build.** The management redesign and OurBanner **share the Edit-household sheet** (the redesign's pencil opens it; OurBanner fills it) — building management without the photo ships an Edit sheet you immediately reopen, so they ship together. **Model: entity/membership** — collection → selected → scoped detail: **Your Households** (name-only rows, **bare pencil on the active row only**) above the line; **`{household} · MEMBERS`** roster + Invite below, recomputing on switch. **Delete lives inside the Edit sheet** (danger zone, creator-only — refines OurBanner D2); **Invite = `navigator.share()` hand-off** (no in-app share UI, no "Copy link"). Spec of record `docs/specs/active/SPEC_household_management_phase1.md` (D1–D8, build sequence, verification) + mockup `docs/mockups/mockup_household_manage_FINAL3.html` (bare-pencil, tiebreaker). **Build sequence:** (1) migration 024 → dev, verify anon+non-member; (2) Edit sheet (photo/zoom/replace/wordmark-segment + name + creator-only Delete danger zone, both states); (3) two-zone management sheet; (4) Invite → `navigator.share()`, remove any self-rendered share UI. One tested commit per step, dev→main held until Phase I fully validated. **OurBanner details (unchanged, `SPEC_household_photo_header.md` D1–D8):** **Migration 024 authored + paren-fixed, NOT applied** — assign at build against the `migrations/` high-water mark. Load-bearing design facts (supersede the 2026-07-16 "three hard requirements"): **(1) framing = position AND scale** — a 2.6:1 band can't be filled by an arbitrary photo with `background-position` alone (`IMG_7897` needs ~165%); store 3 ints/household (`x=40,y=46,zoom=165` reference), non-destructive; **(2) scrim = one principle, two treatments** — the app header uses a **band** scrim (darkens only the two always-type strips, ~4% in the middle), NOT the radial (radial is the *landing hero's* treatment; both linear and radial *assume* centre, band assumes nothing); black, text-shadows load-bearing; **(3) the preview IS the control** — no separate crop screen (`crop_tuner.html` turned out to be the feature); zoom is a **slider, not pinch**; **(4) EXIF normalize on the STORED artifact** (`<canvas> drawImage` ignores the tag; 2/2 test photos carried orientation 6/3); **(5) the group sets the look** — all 5 fields are household state, any member, and join the `activeHouseholdId` swap set. Schema: 5 new `households` columns + CHECK constraints; private `household-photos` bucket, 4 member-gated policies, no anon policy. | `docs/specs/active/SPEC_household_photo_header.md` |

### Other NOW

| # | Feature | Notes |
|---|---|---|
| 1 | *(Per-household staple model — **shipped 2026-07-07** as `household_staples` / migration 016; see DONE.)* | |
| 2 | *(NOW #1 + #2 of last sprint — `auth.uid()` RLS fix and helper consolidation — shipped 2026-06-29 as migrations 014/015; see DONE.)* | |

---

## NEXT — Clearly Defined, Not Yet Started

| # | Feature | Notes |
|---|---|---|
| — | *(**F0 — `uq_live_list_item` partial-unique on `list_items`** — DONE 2026-07-15, migration 020 live dev+prod, `.sql` `0554587`. See DONE.)* | |
| — | *(**F1b — hide→re-add no-stomp** — DONE 2026-07-15, `85f4a69` → main `28539af`, prod-verified. `unhideItem` + `updateQty` resolver hardening + reveal card. See DONE.)* | |
| — | *(**Contributor A-fix — verify on dev + merge to main** — DONE 2026-07-16; `abc5f15`, prod-verified. Name-line renders from the contributor ledger; own items show no name line. See DONE.)* |
| — | **Contributor ledger model — build (BUILD-GATED)** *(NEW 2026-07-15)* | Implement "a badge is the last thing you said" (see DECISIONS 2026-07-15): attribution from utterance, not arithmetic; your badge clears only when *you* reduce by your full ask or reduce to zero; silence ≠ withdrawal; badge count may exceed qty (a household conversation, not a data error). **Gate:** rule (a) needs a per-actor quantity *delta*, but the stepper reports end states — prove the client can attribute a change to an actor under polling/optimistic updates before building. Also verify the spec's claim that migration 009 "solved the wrong problem" (asserted from a code comment). Proposed DB invariant: a live `list_items` row must never carry a contributor ledger that contradicts it, and no user's action may soft-delete another user's contributor row (`list_item_contributors.deleted_at` exists + unused). Then repoint the remove-confirm dialog + `addedByMap` off `added_by` and demote it to audit-only. Per `docs/specs/active/SPEC_contributor_ledger_desync.md`. Supersedes/absorbs the older "Contributor display refinement" + `close_cycle` carry-forward items below. |
| — | **Tune the RUM detector threshold against real traffic** *(monitoring follow-up; NEW 2026-07-15)* | The prod RUM JS-error detector (`rum.client_error.count` > 1, immediate) is set at threshold **1**, backtested to exactly 1 alert in the prior week (the real outage). Untuned against real-world noise — if a tester's flaky connection pages overnight, raise to 2. Confirm against a week of live traffic. |
| — | **Backup cadence decision — floor exists, strategy doesn't** *(infra; UPDATED 2026-07-16)* | Zero-cost **floor is DONE** (2026-07-16: verified off-instance `pg_dump` — see DONE). But *a backup you must remember to take is one you will eventually forget to take* — tonight's dump ages from this moment. Decide the cadence **deliberately**: cron / Supabase Pro plan / accepted risk at beta scale. Pro-plan note: both projects share the Velayo org, so Pro pulls dev onto paid too (~$45/mo quoted). Also parked from the same session: prod Postgres patch (17.6.1.084 → .141) deliberately not taken until the backup existed — safe to take now. |
| — | **Invite-arrival friction — three failures in three layers (AMENDED 2026-07-16)** *(funnel-critical; beta session 2026-07-14, re-diagnosed 2026-07-16)* | 3/3 external testers failed at the link→app seam — a true *pattern* but a **false single diagnosis**. Re-split into three distinct failures in three layers: **Aidan = expectation** (expected a native download — addressed by the front-door copy, now NOW); **Heddi = delivery** (link artifact; likely schemeless `ourprovisions.velayo.ai` vs `https://` — addressed by the referral primitive owning the artifact, below); **Chris = app** (join never activated — its own NEXT item, below). ⚠️ **The 2026-07-14 RCS rich-card root cause is OVERTURNED** — Chris received a carded link on Android and it *worked*, so card+tap is not the cause; Heddi's failure is now unexplained/unreproduced (n=1). One fix cannot close all three; do NOT design as though it can. Gates the CI activation metric (breadth = 10 touch key features; depth = ≥50% invite a second person). Supersedes/absorbs the LATER "Invite-arrival expectation-setting" item. |
| — | **Referral primitive (migration 023 + Preferences share)** *(NEW 2026-07-16; renumbered 021→022→**023** by 2026-07-18 — per `docs/specs/active/SPEC_referral_primitive.md`)* | ⚠️ **Takes migration 023** — 021 (`beta_signups`) and 022 (anon catalog fix) are both applied; the spec's migration-number prose is stale for the **third** time. Confirm the true high-water in `migrations/` and take the next free number at build (don't trust the prose). Ship-worthy alone (kills the hand-composed-link defect that produced Heddi's failure) but will not move the CI metric until the front door exists. **Referral lives in Preferences, NOT the household share sheet** (reverses the 2026-07-14 "one share sheet, two actions" framing) — invite is household-scoped, referral is app-scoped; co-locating cannibalizes the depth metric. Migration 022 adds `users.referral_code text unique` (set by trigger at user creation, reusing the `household_invites` code generator/format; backfill existing rows) + `users.referred_by uuid references users(id)` (nullable; null = organic). Attribution rides the invite seam: `bootstrap_new_user` takes `p_ref_code` the same way it takes `p_invite_code` — ⚠️ hot-path signature change, no-ref case is the regression guard. `sessionStorage` `pending_ref_code` mirrors `pending_invite_code` (Clerk strips URL params for brand-new users). **Metric = `count(*) where referred_by = <id>`** — no counter, no sends table (a send via `navigator.share` is unobservable). **No gamification.** Copy is the sharer's endorsement in a person's voice, not a company pitch; the mission lives on the landing page, not the button. Button label ("Share OurProvisions") still a placeholder — resolve at build or in the nav/affordances session. |
| — | **Blog home — decide where `blog_july_beta.md` lives** *(NEW 2026-07-16)* | The July beta blog post is written (`docs/content/blog_july_beta.md`) and has nowhere to publish. The obvious answer is a **`/blog` route on `ourprovisions.app`** — but that's a build, not a paste, and rides behind the landing page shipping. Decide the home; the content already exists. |
| — | **Mailchimp OUTBOUND welcome email** *(NEW 2026-07-16 — reframed from capture)* | Reframe Mailchimp as **outbound, not capture** — the TOLL questionnaire *is* the capture layer now. The written July update email (`docs/content/email_july_beta_update.md`) becomes the welcome email that carries the arrival instructions into an inbox where they can be found *at the store* (the July blog said five words — "there's no App Store download" — would have saved Aidan the wait). Explicitly NOT the signup capture. |
| — | **Privacy-policy revision for `beta_signups`** *(NEW 2026-07-17 — `velayo-web` / Platform scope, NOT `ourprovisions`)* | ⚠️ **A live public promise contradicts the live policy.** The landing page says *"we read every answer"* / *"every one of them decides something real about what we make next"*; the policy (effective 2026-03-23) describes email-for-waitlist on a brochure site. As of 2026-07-17 `ourprovisions.app` collects `name`, `email`, and five survey responses (one free text) into `beta_signups`, and **Supabase is not named as a processor** — the most significant one we have. Draft revisions to §02/§03/§04/§06 exist (`handoff/DRAFT_privacy_beta_signups.md`, schema-accurate) — **Claude drafts the schema-accurate sections; a lawyer owns the document.** **§07 (`fit_note` + right of access) deliberately left blank** — `fit_note` is Velayo's private assessment of an identified applicant, arguably disclosable under GDPR/CCPA-style access rights; if so, write it as though the subject will read it. Needs counsel. Files to `velayo-web`, not this repo — the draft is stuck in the airlock until that home exists. Same trigger as the `velayo-web` consolidation + docs split: whether one policy covers both the marketing site and the live app is structural. |
| — | **Anon-surface audit** *(security; NEW 2026-07-18 — fallout of the catalog exposure)* | The anon catalog leak proved the signed-out fetch path needs scrutiny. `category_avg_prices` rides the **same signed-out fetch path** — confirm it's an aggregate with **no per-household rows** and no policy of the same broken shape (name asserts a filter the predicate doesn't contain). Broader: **sweep `pg_policy` across all tables** for policies whose *name* claims a scope their SQL doesn't enforce (`"Anyone can read global catalog items"` had no `is_global` check). Verify RLS via `set local role anon` in-transaction or an anon-key PostgREST request — **the SQL editor runs as `postgres` and bypasses RLS; it will lie about both the policy and the data.** |
| — | **Disclosure decision — catalog exposure** *(Dan's call; NEW 2026-07-18)* | Household-custom item text (free-text, incl. a beta user's parent's medication schedule + dated caregiving notes) was readable by any anon visitor with the publishable key for an **unknown window** before migration 022 closed it 2026-07-18. Small F&F group (Helen, Elly, Aidan), **no evidence of access** (no logs proving it either way). Whether/how to disclose to the affected testers is a judgment call, not an engineering one — **Dan decides.** Related: `catalog_items` is now understood as **PII-bearing, not reference data** — any future anon or cross-household read of it needs `list_items`-grade scrutiny. |
| — | **Query Chris's failed join against prod** *(NEW 2026-07-16; carried unresolved since 2026-07-14)* | The only one of the three arrival failures where the app might actually be broken. Stale client view vs. a missing `household_members` row vs. the already-fixed banner-timing race. **Starts with a query, not a design** — has never been checked against prod. |
| — | **Hidden-items findability — residual (grouped hidden matches in search)** *(NEW 2026-07-14; core DONE via F1b 2026-07-15)* | F1b delivered the correctness core: a hidden-but-live **exact** match surfaces as a reveal card (un-hide only) and re-adding no longer forks/stomps. Residual from the beta-session vision: show hidden matches as a **distinct group** in `searchResults` (visible matches first, hidden matches below) with inline per-item unhide, not only the single exact match. Granular unhide falls out of this (no separate unhide-manager needed). Pairs with the **search/filter label** decision — resolve label + behavior together (decide once, or relabel twice). Confirm scope vs. what F1b already shipped before building. |
| — | **Multi-household member-picker (Option B)** *(spec-worthy at build; beta session 2026-07-14)* | Once two users share any household, adding one to another is *selection, not invitation*. Touches membership only; degrades to invite-link for true strangers. **Instant-add but NOT silent** — the added user must see they were added (consent edge: adding grants write access + exposes a household's existence/contents). **RPC trap:** needs "all users who share ≥1 household with me" — a self-referential walk over `household_members` (my households → their members → dedupe → exclude self); given the known `household_members` recursion trap this almost certainly requires a **SECURITY DEFINER RPC** (like `is_member_of` / `get_my_households`). Option A (clone-carrying-items) deferred as unvalidated; if ever built, keep composable with B, not monolithic. |
| — | **Migration numbers — ALWAYS take the next free number at build, never from a spec** *(bookkeeping; UPDATED 2026-07-18 after the third double-claim in four migrations)* | On disk: **021** `beta_signups` (applied), **022** `anon_catalog_global_only` (applied 2026-07-18), **024** `household_photo` (authored, **not applied** — Beat 1 next session). **023 is an intentional gap** reserved for the **referral primitive** (specced, not built). ⚠️ **Do not assign migration numbers at design time** — 021/022/023 each double-claimed because specs stamped a number that moved before build. Rule: `ls migrations/` for the true high-water and take the next free number **when you write the file**, not when you write the spec. Separately: the **009–012 / 017 gaps + duplicate `007`** are **honest drift — do NOT repair by reconstruction** (see the "Reconcile migrations/ folder" item + the 2026-07-16 DECISION). |
| — | **Household-scoped UI state audit** *(headline)* | Enumerate every piece of UI state scoped to a single household and verify it resets on active-household change — fix the whole class at once. Surfaced 2026-06-29 as systemic: join banner, invite link, and a near-miss desync all stemmed from household-scoped state surviving a switch. Deliver an audited list (pass/fix per surface) + fixes for any remaining instances. |
| — | **Member-list / realtime propagation latency** | Already-open clients take ~30s to reflect membership/name changes. Investigate whether `household_members`/`users` have realtime subscriptions or only `list_items` does. Related architecture note: `householdMembers.users.full_name` (embedded client snapshot) can lag the base `users.full_name` source of truth until the stale client re-fetches. Surfaced 2026-07-01. |
| — | *(**"No longer a member" banner leak — promoted to NOW / P0-BLOCKER 2026-07-07; ROOT-CAUSED + FIXED on dev 2026-07-09 → DONE.** Deliberate-loss / watchdog-poll race, not stale storage. Prod-verify after merge is the last gate on the signup-email flip.)* | |
| — | *(**Duplicate same-named households — ROOT-CAUSED + FIXED 2026-07-10 → DONE.** Confirmed via prod queries: true duplicate creation / creation-race (259ms double-tap → two ids). Client button-disable shipped `565b133`. See DONE + `docs/SPEC_duplicate_household_repro.md` / `_fix.md`.)* | |
| — | *(**Remove dead `selfDepartureRef`/`markSelfDeparture` — DONE 2026-07-10** `5da1c37`. Removed + ARCHITECTURE synced.)* | |
| — | *(**Duplicate-household Part 2 — prod cleanup DONE 2026-07-10.** Soft-deleted the later id of the "Test House 200" dup pair on prod (2a→2b→2c); one live household per name, reversible via `deleted_at`. See DONE + DB changes in SESSION_LOG.)* | |
| — | **Splunk-on-local-dev exclusion (needs a decision)** | Local builds pull the `@splunk/*` packages because `src/rum.js` statically imports them; the machine's install is broken and can't be repaired (npm TLS `UNABLE_TO_VERIFY_LEAF_SIGNATURE`). A dynamic-`import()` gate was tried + reverted — webpack didn't dead-code-eliminate the branch on an unset token. Reliable option: gate the Splunk load on `NODE_ENV === 'production'` so `npm start` (local dev server) excludes it via DCE — but it's a prod-telemetry change (async/require init) with tradeoffs, so it needs Dan's call. ESLint is a sound CI proxy meanwhile; deployed builds are unaffected. |
| — | **WATCH — server-side idempotent `create_household`** | Do NOT build yet. Client button-disable (`565b133`) covers the reproduced human double-tap. Promote ONLY if prod shows a duplicate with a non-human `created_at` signature (sub-10ms or retry-interval-spaced) — that means a code-path re-fire (retry hook) is defeating the disabled button. Approach then: dedup on `(created_by, name)` within a short window inside the RPC — a guard, NOT a unique constraint (names are intentionally non-unique). Revisit before any feature makes programmatic (non-tap) create calls. |
| — | **Flip signup email to self-serve "come aboard" variant — UNBLOCKED 2026-07-10** | The false-removal-banner P0 (the last red) is now PROD-VERIFIED, so the cold-start gate is GREEN. Flip the within-the-hour Mailchimp email from the personal variant to the self-serve direct-app-link variant. Both variants already drafted (see `docs/SPEC_new_user_coldstart.md`). |
| — | **Beta 1 launch asset production (NEW 2026-07-09 — strategy locked, assets unwritten)** | Full funnel strategy locked this session (ranked "Come Aboard" primary + "Follow the journey" catch-net; questionnaire reframed application→onboarding-intelligence; Clerk email pre-fill seam verified; activation ladder with invite-as-pivot; two-number success metric = 10 breadth AND ≥50% depth). Produce assets **destination-first / inside-out**: (a) sign-up page + reframed 7-question questionnaire copy → (b) welcome email ("bring your first mate" — the single highest-leverage asset, since invite-email success IS the 50% depth metric) → (c) Mailchimp SEGMENTATION (beta in-app users vs journey-followers — separate segments or nudges misfire) → (d) July blog post → (e) existing-user email + social announcement. Nurture split nurture-now vs nurture-later (Beat 1/2 features become "we heard you" emails on ship; receipt scanning is OFF the beta-10 set). CAVEAT: confirm the Clerk pre-fill query-string param name against the installed version at build. Blocked-behind: the tech-debt pass that makes the invite/CI pivot solid. |
| — | **Native-only advantage: orientation lock (Phase 5 / Expo)** | Field-tested 2026-07-07 (real Sacandaga/Hannaford shop): screen flips one-handed while pushing a cart. Web/PWA cannot lock orientation on iOS Safari — interim workaround is OS Control Center rotation lock; the real fix is a Phase 5 native (Expo) orientation lock. Log as a named native-only advantage, not a web bug. |
| — | **Shop timer — front edge of Phase 2 shopping-sessions** | Field-requested 2026-07-07. Build as the FRONT EDGE of Phase 2 shopping-sessions, NOT a throwaway: start/stop = session boundaries; elapsed time = a stored metric feeding Phase 4 gamification. Built once, with real session boundaries. Pairs with the rotation-lock coach-mark below (shared "start shopping" trigger). |
| — | **Rotation-lock coach-mark** | Rides the shop-timer's start trigger — tapping "start shopping" is the one moment a rotation-lock tip is welcome. Reuses the localStorage-counter coach-mark pattern from the PWA install coach-mark (`docs/SPEC_pwa_install_coachmark.md`). Collapses with the shop timer into one surface. |
| — | **Profile-heading long-term: seed `profileName` from `users.full_name`** | Seed `profileName` from `users.full_name` (single source of truth) rather than Clerk's uncomposed `fullName`. Option A (compose from Clerk parts) shipped 2026-07-01 as the immediate fix; this is the principled follow-up. |
| — | **`bootstrap_new_user` overload cleanup** | 4 overloads exist in prod; only the newest is name-aware. Drop the stale ones (same class as migration 015). Query current overloads via `pg_proc` by `proname` + `pg_get_function_identity_arguments` before dropping. Prod DB change → own spec. |
| — | **Wire `ourprovisions.app`** | Make `ourprovisions.app` reachable + canonical: Cloudflare DNS + Vercel primary-domain + Clerk allowed-domain/redirect; redirect `velayo.ai` subdomain → `.app`. Stay auth-neutral and reversible (auth-domain unification is Phase II). Pre-step: confirm what `velayo.ai` root + `ourprovisions.velayo.ai` serve before retiring/redirecting. |
| — | **`SwipeToRemove` deferred close-gestures** | Shipped 2026-06-29 (see DONE): search-parity (search rows now wrap `SwipeToRemove`), swipe-right-to-close past the 60px threshold, and the `pointerEvents`-on-open unblock. **Remaining, deferred per "wait until users complain":** tap-away to close, single-open-at-a-time (needs lifted/shared state across instances — own session), velocity-based flick. Dan will watch whether users naturally swipe-right vs. reach to tap before building any of these. |
| — | *(**Manage-household surface redesign — DESIGNED 2026-07-18 → folded into Phase I (NOW).** The 2026-06-29 direction ("re-sort by frequency × gravity; heavy actions get a quiet home; stop tangling household vs member actions") is resolved by the entity/membership model: household CRUD above the line, membership below, Delete moved inside the Edit sheet. Now `docs/specs/active/SPEC_household_management_phase1.md` — build is the Phase I NOW item above.)* | — |
| — | **Phase II — Events dimension (design-first)** *(NEW 2026-07-18)* | Phase I's household management deliberately builds the structure Events reuses (collection → selected → scoped detail); Phase II generalizes it. Open questions to resolve in a design session: **(1) Context model** — households + events as one entity with an **optional parent** + **optional deadline** (an event may be hosted under a household or stand alone); **(2) temporary non-inheriting event membership** as a new relationship, and its RLS implications (event members must NOT inherit the parent household's access); **(3) photo-as-context-identity** generalizing OurBanner beyond households — migration 024 ships photo/framing/wordmark on `households`, but the photo is really *context* identity; do NOT re-architect 024, just leave room. Motivating standalone case: Cassie's bachelorette at a hotel — no household, top-level, the photo IS its identity. Overlaps/absorbs the LATER "Event as a second provisioning context" concept. |
| — | **Browse filter reset on household switch — FIXED on dev `dbb57f2`, pending verify+promote (2026-07-11)** | The declutter build's dropped "commit 0" is now implemented: `setSelectedCategories(new Set())` + `setStapleFilter(false)` added to the `[view, activeHouseholdId]` effect, so filters reset on tab/household switch (closes the household-scoped-state leak — stale filter under hidden pills). On dev only; **remaining: dev-verify in a 2-household account (phase-1 switch → phase 0, pills visible, no filters, full catalog; phase-0 filters clear on switch) → dev→main.** One more instance of the deferred household-scoped-state audit class closed. |
| — | **Nav / affordances design decision** *(design session — post-bugs; NEW 2026-07-11; augmented 2026-07-14)* | Bottom tab bar vs. sticky-top header: field-testing found the espresso-header tabs aren't read as tabs, nav is unreachable from a long scroll, the browse→list connection is invisible, and swipe-to-delete is undiscoverable. One structural move (e.g. a bottom tab bar) may resolve 3 of 4. Decide "make existing swipe discoverable" vs. "add a long-press layer" — must reconcile with the existing 4-spec swipe cluster, not add a fifth fighting them. First instance of the ★ invisible-affordances pattern (see DECISIONS). Chat session, sequenced AFTER the shared-list bug fix. **Now carries THREE converged findings (beta session 2026-07-14):** they collapse onto ONE *per-item action surface* — if long-press becomes "do something with this item," then hide / **priority-signal** (must-have/skippable, `list_items`-layer, 3-state) / remove / edit all live in that one menu. Swipe carries dating-app dismiss/reject semantics that conflict with "open item actions" (3 testers reached for long-press). Design as one surface, once — designing separately produces an incoherent gesture system. Mockup-before-code. |
| — | **Spec-path reference sweep (post-reorg)** *(Velayo OS hygiene — NEW 2026-07-11)* | The reorg (`0303397`) moved ~43 specs under `docs/specs/{active,built,retired}/`, so the ~24 `docs/SPEC_*.md` / `docs/specs/SPEC_*.md` references still scattered through ROADMAP + ARCHITECTURE now point at stale paths. NOT blanket-rewritten this pass — several are woven into prose that *discusses* the old path (e.g. migration-reconcile + this very item), so a blind sed would mangle meaning. Do it as a careful targeted sweep: update live NOW/NEXT links first, leave historical/DONE prose that intentionally names the old location. |
| — | **Airlock wiring for spec lifecycle folders** *(Velayo OS hygiene — deferred from reorg, Dan's call)* | The reorg moved existing specs but deliberately left the airlock unwired ("move existing specs, wire airlock later"). New specs still land at `docs/` root, not `docs/specs/active/`. To finish: edit CLAUDE.md (document the lifecycle convention + default-to-`active/`) and `handoff/DESIGN_CHAT_handoff_prompt.md` (airlock `SPEC_*.md` payloads route to `docs/specs/active/` on merge). |
| — | **velayo-os process-doc relocation (unfinished reorg leg)** *(Velayo OS hygiene — NEW 2026-07-11)* | The reorg's velayo-os leg couldn't execute: `velayo_os_flight_checklist.html` isn't present anywhere in the ourprovisions repo (locate it), and the manifest wanted `DESIGN_CHAT_handoff_prompt.md` moved to `velayo-os/docs/` — but that file is a **protected airlock baseline** CLAUDE.md forbids moving. Reconcile: decide whether the OS handoff prompt truly belongs in `velayo-os/docs/` (and if so, how, given the airlock rule) or stays put. Cross-repo move (separate git repo) — not a `git mv`. |
| — | **`ourprovisions.app` single-voice landing page** | Sine-wave flow: Boardroom (savings rational) → Trough → Ah-ha → Vision → Front Door. **Front door = Mailchimp capture (questionnaire dropped for beta).** Hero copy to final (uses `EVIDENCE_grocery_savings.md`); Movement II placement pattern resolved. Per `docs/mockups/mockup_come_aboard.html`. |
| — | **Patch Vision Roadmap with JCB savings citation** | `Velayo_Harbor_Investor_Narrative.docx` carries no external savings citation — only illustrative in-app figures. Add Davydenko & Peetz (2020), *J. Consumer Behaviour* anchor: list-makers spend ~$10–13 less per trip. See `EVIDENCE_grocery_savings.md`. |
| — | **`beta_signups` mission-control table (migration 018 — renumbered from 017; 016/017 now taken)** | Table #1 of the mission-control layer (Velayo-owned telemetry). **Questionnaire dropped for beta front door; `beta_signups` retained as the store for any future structured intake (post-Mailchimp-phase).** Per `docs/specs/SPEC_beta_signups.md`. |
| — | **Receipt import v1 (photo)** | Feature fully designed across 4 specs in `docs/specs/` + review mockup in `docs/mockups/`. Build sequence: (1) prove photo→JSON vision extraction on a real receipt in isolation before any DB work; (2) write migration 017 (`receipts` + `receipt_items` tables + 4 pre-seeded columns) — *renumbered from 016; that number is now `household_staples`, applied 2026-07-07*; (3) build reconcile + review UI; (4) commit flow + `price_hint` rolling-average refresh. Confidence-gated review (≤0.80 surfaces to human; above auto-commits but stays auditable). NO `list_items` write in v1. Source-agnostic core — email/API adapters are Phase 2+. |
| — | **Create household with cloned catalog** | Build `create_household_from_template` per `docs/SPEC_create_household_from_template.md`. New RPC wraps `create_household` (006); clones source household's custom catalog into the new one (snapshot, not live link); null source = passthrough ("Standard provisions"). Client: dropdown picker in manage-household sheet. Resolve item-count RPC shape + most-recently-active default during build. |
| — | **Reconcile `migrations/` folder** | Fix the `007` numbering collision (`docs/007_dev_restore_role_grants.sql` — a 007-numbered migration filed under `docs/` — vs canonical `migrations/007_finish_authorize_sweep.sql`); recover/locate `009`–`012` (described in docs and confirmed live on prod but absent from local folder); enforce gapless ordering. **2026-06-29:** `014`/`015` now on disk, so the high-water mark is `015`; the `009`–`012` gap and the `007` collision still stand (confirmed by harness Part C — C2). Prerequisite for Supabase CLI workflow and agent test harness Part B. |
| — | **Replace native `window.confirm()` with branded modal** | 3 call sites in App.js (leave household, remove member, ~App.js:2284). Native browser confirm is unbranded — "ourprovisions.velayo.ai says…" box clashes with Layer 2's branded removal notice. Reuse the existing `showResetConfirm` modal pattern; preserve existing on-brand copy. |
| — | **Wire agent test harness — Part C + Part A** | Part C (static repo checks): no DB needed, Claude Code can run today. Part A (read-only prod gate): paste queries into prod SQL editor before each `dev→main`. Both runnable with near-zero setup. See `qa/agent_test_harness.md`. |
| — | **Migrate join banner into `systemMessage` channel** | Precondition (2nd real tenant) met — removal notice is now a shipped tenant. Scope: migrate `joinBanner` state into the typed `systemMessage` channel; decide queue/priority model (newest-wins vs. persistent vs. category-priority); match auto-dismiss parity with the removal notice. UX intent: the green "You joined …" banner auto-dismisses like the bottom notice. Do NOT bundle into the dev→main merge — own session. |
| — | **Disambiguate duplicate-named households in UI** | `households.name` has no unique constraint (by design — two real households may share a name). When names collide in the switcher, show creator name or creation date to distinguish. *(2026-07-10: the "is the second one even real?" question is resolved — the observed dupes were a creation-race, now fixed + being cleaned. This item is now purely about genuinely-real same-named households, if/when they occur.)* |
| — | **Constraint-name reconciliation (dev↔prod)** | `list_items` unique constraint is named differently across environments — dev: auto-generated `list_items_household_id_catalog_item_id_key`; prod: explicit `list_items_household_catalog_unique`. Migration 008 uses the column-target form to dodge this. Any future ON CONSTRAINT migration must account for the split. Reconcile via a dedicated migration. |
| — | **Quiet quantity-bump race** | Simultaneous +1 quantity increments on an already-existing `list_items` row serialize via Postgres row lock (no error) but can land as a single increment rather than summing — silent undercount, no toast. Concurrent-INSERT race is now closed (migration 008); this concurrent-UPDATE race on existing rows remains open. |
| — | **Surface "invite no longer valid" on invalid/spent code** | Invalid or expired invite currently fails silently — user lands in blank "My Household" with no feedback. `bootstrap_new_user` detects the failed join; surface it to the client. |
| — | **Re-key join detection off `joined_via_invite`, not household name** | Join-banner effect in `App.js` checks `household.name !== "My Household"` — fragile if a real household is named "My Household". Re-key off the `joined_via_invite` boolean already returned by `bootstrap_new_user`. |
| — | **Receipt scan entry point in wrap-up modal** | After rolling items forward, prompt appears: "Scan your receipt to capture prices." Natural on-ramp to Phase 3. |
| — | **Household member administration UI** | View members, remove members, manage/revoke invites. Absence is why orphaned memberships accumulated and needed raw SQL to fix (Jun 12). |
| — | **Contributor display refinement** | Keep the full name of the original adder; when another member adds quantity, append their badge rather than replacing attribution with an icon. |
| — | **Wire Harbour live data** | C-suite seat URLs (Claude project links), tool chips (Banking, Cap table, Social, Brand deck), real lane content (priorities, agent counts, "moved Xd ago" dates). Push live once done. |
| — | **Verify migration 005 is applied to prod** | Column inventory suggests `list_items` lacks `cycle_id` — strong signal 005 was written but never run. Confirm; if unrun, apply as a tested migration. Prerequisite for the store-awareness arc. |
| — | **Store-awareness arc (VERIFY-AND-SURFACE, post-multi-household)** | Foundation already designed in migration 005 (`known_stores`, `provision_cycles`, `shopping_sessions`, `match_known_store` RPC, Scenario D silent GPS auto-detect). First step: confirm 005 is live. North-star payoff: N3-style price steering ("cheaper at Market Basket") — build only once price-history data is dense enough to be honest. |
| — | **Verify Velayo OS project instructions are on v2 handoff flow** | The Velayo OS project's own instruction field may still carry v1 Drive-writing Scribe language (flagged in 06-11 Harbour entry). Dan to check and apply same v2 replacement. |
| — | **Auto-stamp lane "last moved" dates** | SESSION END writes the active lane's date on close — self-maintaining neglect detector in the Harbour. |
| — | **Reconcile Vercel env scopes** | Development scope still carries prod Supabase vars (79-day-old). Repoint/remove to dev; add `REACT_APP_CLERK_PUBLISHABLE_KEY` to Preview. Unblocks `vercel env pull` as clean secrets-distribution route. |
| — | **Stand up Bitwarden for secrets** | Replace Google Drive `.env.local` stopgap with Bitwarden. Delete the Drive copy once migrated. |
| — | **"Fabric Softemer" orphan row cleanup** | Free-typed list row has no `catalog_item_id`. Rename or delete it on prod so it doesn't surface as a broken state for other members. |
| — | **Roll-forward orphan guard** | Decide whether `close_cycle` should block (or skip) items with no `catalog_item_id` to stop new orphans accumulating across cycles. |
| — | **Decommission "Reset Public Schema Permissions" query** | This saved query stripped all `authenticated`/`anon` grants on the dev sandbox (Jun 16 incident). Rename to "⚠️ DANGER — strips all grants" or delete from both dev and prod SQL editors. |
| — | **Add "commit + push after edits" directive to CLAUDE.md** | Stranded commit at session start caused a Vercel-stale-build false alarm. Add explicit rule: after any code edit, commit + push before closing the session. |
| 4 | **Cascade soft-delete (catalog → list)** | Deleting a custom catalog item should cascade to active `list_items` rows. Same gesture as the Delete half of #2 — pairs with it. |
| 5 | **Fix close_cycle contributor carry-forward** | When rolling items forward, copy `list_item_contributors` rows to new `list_items`. Currently badges reset to `added_by` only after wrap-up. Fix is in the `close_cycle` RPC. |
| 6 | **Re-enable RLS on provision_cycles, shopping_sessions, known_stores** | Confirmed disabled in prod (matches dev). Anon key can cross-household read/write these tables. Low stakes now; address during migration rewrite. |
| 8 | **Global category rename** | `household_category_overrides` table. Lets a household rename "Pantry" → "Dry Goods" etc. Migration pending. |
| 9 | **Reset Household (nuclear option)** | Confirmation-gated. Returns catalog to factory seed and clears household customizations. NOT the everyday undo for hides/deletes — recovery-from-chaos only. |
| 10 | **Replace remaining `window.location.reload()` calls** | Audit codebase; replace all with `refreshCatalog()` pattern. |
| — | **Avatar two-letter monograms (small UX)** | Single-initial contributor badges are ambiguous — two users can both render "D" (Dan Holmes / Dan Test User). Fix: two-letter monogram (DH / DT — names already provide the data). Alternatively: per-user distinct colors, or lean on the existing hover-name. Two-letter likely cleanest. |
| 11 | **Email receipt parser** | Most actionable near-term price ingestion path. No partnerships required. Parse forwarded grocery receipts via email. |

---

## LATER — Phase 1.5: Active Household Identity

| Feature | Notes |
|---|---|
| **Phase II shared active-household lens at the harbour/identity layer** | Once the Harbour / `velayo-os` identity layer exists, the "which household is active" signal should be a cross-app primitive shared by all Velayo apps (OurChef, OurManifest, etc.) — not re-implemented per app. Phase I outer-chrome indicator is the OurProvisions-only stopgap; Phase II retires it in favor of the shared lens. Scope belongs to a `velayo-os` session once app #2 starts. |
| **"Your data is yours" editable profile surface** *(banked future beat — 2026-07-09; earns a SPEC when built)* | A "we heard you" beat, not now. Onboarding data belongs to the user: it should live under their name in-app, be editable, be labelled with *why* it exists, on an honest "keep sharing and it gets smarter" loop — Velayo's data posture made visible. Technical shape when built: a **separate editable profile table**, user-owned and mutable (RLS scoped to the user), **seeded from `beta_signups` but never conflated with it** — `beta_signups` stays the immutable, timestamped intake record (founder-only `status`/`fit_note` intact). Do NOT make the intake row user-editable. Small schema decision with a real "why" + verification need → write a proper SPEC when the beat comes up. See ARCHITECTURE "Banked design principle — Your data is yours". |
| **Households in Preferences** *(backlog — clarify first; from field test 2026-07-11)* | Aidan asked for households in Preferences — unresolved whether he meant "**manage** my households" vs. "**switch** active household." Clarify with Aidan before speccing; likely rides the editable-profile / Preferences beat. |
| **Invite-arrival expectation-setting** *(→ PROMOTED to NEXT 2026-07-14; re-diagnosed 2026-07-16)* | Set the arriving invitee's expectations on first open: "this is the app; add it to your home screen." Cheap and funnel-critical (protects the depth/second-invite metric). Pairs with the existing PWA-install coach-mark + cold-start specs. Now split across the NEXT items (front-door copy → NOW; referral primitive; Chris query). |
| **Android link-format repro (schemeless vs `https://`)** *(NEW 2026-07-16; 5 min, non-blocking)* | Cheap open test that would settle Heddi's unexplained failure: text an Android phone `ourprovisions.velayo.ai` vs `https://ourprovisions.velayo.ai` and tap both. Leading hypothesis is that Dan's hand-sent link was schemeless where the in-app invite carries `https://`. The referral primitive removes this defect surface *by construction* (app owns the artifact) whether or not the repro is ever run. |
| **Landing hero video (boat / moving water)** *(NEW 2026-07-16; deferred on page weight)* | Swaps into the existing landing-hero image layer with the same scrim — one asset, not a redesign. Deferred until the page is proven; ship the photo first. |
| **Future-state landing hero — swap to the household photo header** *(NEW 2026-07-16)* | Once Beat 1 (household photo header) ships, swap the landing hero's device mock to the real household photo header. One asset swap, not a redesign — the landing hero and app header are the same scrim primitive at different scales. |
| **Event as a second provisioning context** *(named strategic concept — beta session 2026-07-14; ~Phase 4, own design session)* | Household = standing/permanent; event = bounded/ad-hoc/deadline-driven (golf trip, bachelorette). May express the CI thesis *more* vividly than the household (acute pain, unavoidable collaboration, natural referral surface, easier on-ramp). **Modeling fork (unresolved):** first-class entity with its own lifecycle (planning→active→closed) and a **"claim" / "I'm bringing this"** verb that *divides* responsibility rather than sharing one pot — OR simply a household with an expiry/archive + a flavor. Lean: cheapest first (event = temporary household), validate before promoting. Note the loop: "claim" and priority-signaling both answer *who owns getting this item*, so the event verb may inform the household priority model. No change to Phases 1–3. |

---

## LATER — Velayo OS / Infrastructure

| Feature | Notes |
|---|---|
| **Splunk OTel Collector on AWS Lightsail** *(deferred to Phase 3–4; NEW 2026-07-15)* | Server-side DB/infra telemetry (would have shown a healthy Supabase during the 07-15 outage — real value, but not the bottleneck). Splunk's free 15-host tier removes the cost objection, not the attention cost: a VM to patch, a Compose file, a scrape config that fails silently. **Architecture design is sound — keep it for when it's *wanted*, not *needed*.** RUM + Synthetics already deliver ~80% of the benefit with zero operational surface. Deferred deliberately: "a fun afternoon, not a fire drill." |
| **Migrate off Supabase legacy anon/service_role keys** *(watch-item; NEW 2026-07-15)* | Legacy keys are on a deprecation path toward Publishable/Secret keys. Not urgent — the app uses the legacy anon key and the Synthetics uptime check matches it (so both move together). Revisit when Supabase forces it; update the Synthetics `apikey` header in lockstep. |
| **Consolidate multiple Supabase client instances to one shared client** *(design-first)* | `useProvisions.js` and `ActiveHouseholdContext.js` each create a Supabase client sharing one auth storage key → persistent GoTrueClient multiple-instance warning. Confirmed SEPARATE from the name-change hang (2026-06-29). Non-urgent; auth/token-flow blast radius means a design session before touching. |
| **Required first/last name at signup** | Clerk config + backfill + social-auth check. Reduces email-prefix display fallbacks; decided 2026-06-29 this makes the rare unnamed-member case a non-issue rather than something to engineer around. |
| **Show which household an invite link is for, in the Share panel** *(ExD polish)* | "Invite to <name>" in the Share panel — makes the household↔link binding legible and prevents stale-link confusion structurally (complements the 2026-06-29 stale-link reset). |
| **Authenticate/link the `vercel` CLI for Claude Code** | So deploy status can be verified directly instead of gating on a human dashboard check. Surfaced 2026-06-29 when promotion couldn't confirm prod-build-green programmatically (no `gh`, `vercel` CLI not authed/linked). |
| Promote "Crew only" to a reusable Access Group | When a 2nd internal app or 4th person appears — scales without per-hire edits. |
| Split company log into `velayo-os/docs` | Trigger = app #2's first session, not a date. Filter-based split, not a migration (scope tags already in place). |
| Sync on unfocused tabs | Browser `setInterval` throttles to ~1 min on backgrounded tabs, causing perceived lag. Consider `visibilitychange` refresh-on-focus, or Supabase Realtime if Clerk/Realtime auth incompatibility is ever resolved. |
| Invite-accept creates duplicate memberships | Accepting an invite adds a membership without retiring the user's auto-bootstrapped household. Should move-or-block until multi-household is real. |
| Apply migrations 001 + 002 to prod | Required before multi-household ships to real users. Currently dev-only. |
| **Location-confirm first-landing** *(gated behind store-awareness arc)* | When location is shared and a confident GPS match exists, offer a one-tap confirm ("You're in Day, NY — shopping for NewLeaf?") — never a silent switch. Location gets a voice, never a vote. |
| Remove temp `[ActiveHousehold TEST]` console log from `App.js` | Added for spine verification; remove before multi-household UI ships. |
| Audit SECURITY DEFINER functions for `set search_path` | `get_my_households()` and `get_household_id_for_current_user()` lack explicit `set search_path`. Low risk now; address when consolidating helper functions. |
| `bootstrap_new_user` step-1 dead-logic cleanup | `if v_user_id is null` guard is always true because the INSERT above never sets `v_user_id` (no RETURNING clause). Harmless but misleading. Fix when rewriting bootstrap as the real active-household fix. |
| Reconcile git remotes across machines | Canonical remote is `Velayo-ai/ourprovisions`; stale Surface clone pointed at `dan-velayo/ourprovisions`. Reconcile when doing next per-machine setup. |
| Local Supabase (CLI/Docker) for offline dev | Seeded from `000_canonical_baseline.sql` — for genuinely-offline boat development. Only if offline dev becomes a real need. |

## LATER — Catalog / View Refinements

| Feature | Notes |
|---|---|
| Multi-item / category-level Hide | Hide several items (or a whole category) in one action. Build ONLY if user feedback shows one-by-one hiding is a real pain. Deferred by decision June 8. |
| **Shop filter-pill bar (who-added, per-store)** *(horizon — surfaces when stores are introduced)* | Shop gains its own filter-pill bar separate from the declutter cycle: filter-by-who-added (Elly/Helen/DH) and per-store filtering (split a trip across stores; hide the other store's items). The cycle handles the *view* axis (grouped/flat + hide-noise); pills handle the *content* axis (what's shown). Captured in `docs/SPEC_declutter_cycle.md` future section. |

---

## LATER — Phase 2: Know What's in the House

Shopping intelligence layer. Captures behavior silently and learns from it.

| Feature | Notes |
|---|---|
| Shopping sessions — "I'm going shopping" UI | Start session explicitly, or auto-open on first check-off. GPS captured silently. |
| Store detection | GPS match against `known_stores`. Receipt scan confirms. Manual fallback only for new stores. |
| Smart list ordering | Sort list to match user's natural path through their store. Per-user, per-store. |
| Pantry scan | Camera → Claude Vision → home inventory populated. |
| Pantry layer | "What's in the house" view, distinct from the shopping list. |
| Inventory → List nudge | Running low? App suggests adding to list. Closes the loop. |
| Impromptu trip UX | Quick-add mode: fast scratchpad, receipt scan at end, closes same day. |

---

## LATER — Phase 3: Price Intelligence

The intelligence layer. Prices are infrastructure, not UI. Everything feeds from receipt scanning.

| Feature | Notes |
|---|---|
| Receipt scan (camera) | Capture receipt image → Claude parses → populates price_history |
| Email receipt parser | Forward grocery email receipts → parse → price_history |
| Price history | Per item, per store, over time |
| Cross-store comparison | "Eggs are $1.20 cheaper at Market Basket" |
| DB: `receipts` | `id, household_id, scanned_by, store_id, raw_image_url, parsed_json, total, purchased_at` |
| DB: `price_history` | `id, household_id, catalog_item_id, store_id, price, quantity, scanned_at, receipt_id` |
| Strategic partnerships | Fetch, Ibotta (receipt aggregators); Market Basket, Hannaford, Whole Foods; Apple Wallet, Gmail |

---

## LATER — Phase 4: Smart Nudges & Gamification

Closes the loop. Turns data into action.

| Feature | Notes |
|---|---|
| Smart nudges | "You usually buy milk this time of week" |
| Savings framing | "You saved $14 this month" — unlocks better living, not just spending less |
| Waste intelligence | "You've wasted spinach 3 times — try buying less" |
| Household milestones | Meaningful moments > points systems. Contextual, earned. |

---

## LATER — Phase 5+: The Fleet

| App | Notes |
|---|---|
| **OurChef** | Meal planning integrated with OurProvisions list |
| **OurGarden** | Home garden tracking; compares harvests vs. grocery savings |
| **OurManifest** | Provisioning for sailing trips; crew drawn from Harbor family |
| **OurDiscovery** | Family experiences layer |
| **OurHelper** | Neighborhood mutual aid — share lists with a helper |
| **Data Marketplace** | Opt-in anonymized household behavior data. One consent field unlocks the future. |
| **Child mode** | Visual picture-based interface for kids; requests land on parent list |

---

## DECISIONS LOG

| Date | Decision |
|---|---|
| 2026-07-18 | **Household management adopts the entity/membership model (collection → selected → scoped detail).** Household CRUD above the line, this-household membership below, one selection driving both zones (membership recomputes on switch). Rationale: separates the household-as-entity from the people in it, and deliberately builds the structure Phase II Events reuses — a "Your Events" sibling section drops in without re-architecting. |
| 2026-07-18 | **Active-row affordance is a bare pencil (FINAL3), not a pill; the "ACTIVE" word is removed.** Selection already reads from espresso fill + clay ring + header echo; the "Edit" label carries the action without chrome, keeping the row-action pattern uniform (trash + Edit both bare, right edge). A simpler rule than "icon-only bare, labeled gets a pill." Pill variant (FINAL2) was built and rejected. |
| 2026-07-18 | **Delete household moves into the Edit sheet (danger zone, creator-only), refining `SPEC_household_photo_header.md` D2.** Under the entity model, Delete is an operation on the household and belongs with its other edit-actions — you go *into* the thing to destroy it. "Created by you" resolves to the creator-only *visibility* of Delete (D4), not a header label. Permission unchanged: creator-only. |
| 2026-07-18 | **Invite transport = the OS share sheet (`navigator.share()`); the in-app share UI and "Copy link" are removed.** The persistent banner *was* the bug; removing the in-app surface removes the defect class by construction, and simplifies the code (verify the app calls `navigator.share()`, not a self-rendered sheet). The share sheet already offers Copy. |
| 2026-07-18 | **Brand principle: maritime is expressed through language and imagery (place names, "aboard"), not repeated decorative glyphs.** Anchor/monogram placeholders dropped; household rows are name-only — the name is the identity, the photo lives in the header and Edit sheet, not repeated in the switcher. |
| 2026-07-18 | **A policy's name is not its predicate.** `"Anyone can read global catalog items"` had no `is_global` check and leaked every custom row to anon. The name documented an intent the SQL never implemented, and it survived because RLS was correct everywhere else. **Sweep `pg_policy` across all tables for names that assert filters their predicates don't contain.** |
| 2026-07-18 | **A query asks for what it wants; RLS is the backstop, not the filter.** The anon fetch requested every row and relied on RLS to decide what came back. Relying on RLS as the *primary* filter makes every policy load-bearing for correctness, not just security — and when both layers fail, the failure is silent and the blast radius is the whole table. |
| 2026-07-18 | **`catalog_items` is PII-bearing, not reference data.** Users treat free-text item names as a scratchpad — the leak exposed a parent's medication schedule and dated caregiving notes, not groceries. Any future anon or cross-household read of this table needs `list_items`-grade scrutiny. |
| 2026-07-18 | **The SQL editor cannot verify RLS.** It runs as `postgres` and bypasses policies — it returned all 183 rows before *and* would after a correct fix. Verify via `set local role anon` in-transaction, or an anon-key request through PostgREST. The editor will lie about both the policy and the data. |
| 2026-07-18 | **Keep anon browse, scoped to `is_global = true`.** Considered dropping the anon policy entirely. The signed-out browse of the 50 seed rows is a real part of the landing experience and the seed rows are Velayo's own content with no household attached. The risk was never anon browse — it was anon browse of a table that *also* holds user text. If the landing page ever needs richer data, build a purpose-made view; don't widen this policy again. |
| 2026-07-18 | **OurBanner: the group sets the look.** Photo, framing, banner state, and name are household state; any member changes any of them. Reversed an in-session lean toward a per-user preference — a private reskin of shared chrome is the one place the app would quietly stop being *ours*. Accepted cost: Helen changes it, Dan's header changes with no attribution — exactly how the household name already behaves. |
| 2026-07-18 | **Delete household stays creator-only and stays in the switcher modal.** Held deliberately against a broader "anyone can do anything" rule. Deleting isn't editing — it ends a thing three people use. Keeps `"Created by you"` meaningful, and keeps the existing `window.confirm()` at its current risk level rather than making it more dangerous. |
| 2026-07-18 | **The banner control only exists when a photo exists.** No photo → no control, wordmark renders large, empty-band case unreachable by construction. The setting appears at the moment it becomes meaningful. This rule is what let "hidden" mean hidden with no fallback lockup. |
| 2026-07-18 | **When three consecutive fixes to one idea each introduce a new defect, the idea is wrong, not the execution.** "Hidden" was designed four times: name-promotes-to-hero → long-name overflow; corner mark → legibility depended on the user's photo; relocated corner mark → displaced the tab bar. All three were downstream of the hero promotion. Removing the first domino removed the chain. |
| 2026-07-18 | **Zoom is mandatory, not polish.** A 2.6:1 header band cannot be filled by an arbitrary photo with `background-position` alone — `IMG_7897` frames correctly only around 165%. Beat 1's three fixed slices could not express this. Framing = position **and** scale, both persisted. |
| 2026-07-18 | **Assign migration numbers at point-of-build, not design time.** Third double-claim in four migrations (021 `beta_signups`/referral; 022 referral/anon-catalog; 023 referral/photo-header). Specs claim a number at design time and it moves before build. Check `migrations/` for the true high-water mark before writing any number into a file. |
| 2026-07-18 | **Referral primitive renumbers 022 → 023.** The anon catalog fix took 022 and is applied; the photo header took 024. ROADMAP prose saying "migration 022" for referral is now stale for the second time — use 023 (or the true next-free at build). |
| 2026-07-17 | **Heading and body must do different jobs.** Every awkward caption was two sales pitches stacked — heading made a claim, body made the same claim longer. The invite caption always worked because its heading is an *instruction* and its body explains. Rule: heading states, body explains, each caption owns exactly one idea. A caption carrying two stories is what reads as awkward. |
| 2026-07-17 | **Don't use a product term as a plain-English word.** "Staples" is a filter chip, a table (`household_staples`), and a user action. In a heading it reads as the button; in the body, after the plain idea lands, it reads as the name for the thing. Heading gets the person's words ("the stuff you buy every week"), body gets the feature word — teaching without a lesson. |
| 2026-07-17 | **Speed is a claim the page had never made.** The thesis frames the shop as the pain, but no caption said the app makes the trip *faster* — the most obvious thing a shopper wants. Guardrail: the app doesn't make you faster, a *list* does — so the body says "as you move" rather than boasting "record time." |
| 2026-07-17 | **Legal pages are Velayo's — one canonical copy for the fleet.** The landing footer links out to `velayo.ai/privacy` and `velayo.ai/terms` with absolute URLs rather than copying them per app. Copying legal text onto each front door is the divergence problem at 100 apps, and it starts at app one. |
| 2026-07-17 | **Footer uses `grid-template-columns:1fr auto 1fr`, not flex `space-between`.** Equal outer columns hold the middle at the page's true centre regardless of side widths; flex drifts toward whichever side is shorter. |
| 2026-07-17 | **★ Judge the live page in the real browser, not a mockup in an in-app viewer.** Four rounds were spent "fixing" a dark-mode bug that didn't exist — Claude's in-app browser was theming a downloaded HTML file. `ourprovisions.app` in Safari was correct the whole time. **Check the surface before debugging the render.** Same family as "truth is a query against the correct environment," extended to what you're *looking through*, not just what you're querying. |
| 2026-07-17 | **The privacy policy contradicts a live public promise.** The landing page says *"we read every answer"* and *"every one of them decides something real about what we make next."* The policy (effective 2026-03-23) says we collect an email address for a waitlist. Two of our own live surfaces disagreeing — not a compliance nit, and only visible to someone who reads both. |
| 2026-07-17 | **Claude drafts the schema-accurate sections; a lawyer owns the document.** The hard part of a privacy revision is knowing exactly what's collected — a good use of an assistant that knows the schema down to the column. Representations about how a Delaware corporation handles personal data are not. §07 (`fit_note` + right of access) left blank deliberately. |
| 2026-07-15 | **Do not upgrade Supabase compute.** Reverses an earlier recommendation made before seeing real IOPS. Prod runs at ~0.1% of disk IO capacity (3 of 3,000 IOPS); upgrading would have paid to fix a load problem that does not exist. Free tier / nano is adequate at current scale. |
| 2026-07-15 | **Alert on user-visible failure, not infrastructure metrics.** The outage proved it: infra metrics (Disk IO) screamed and were wrong; the user-visible signals (RUM JS errors, 522s) were quiet in an unwatched dashboard and were exactly right. Detectors go where the user lives. |
| 2026-07-15 | **Synthetics must exercise the Data API, not the homepage.** Vercel served the HTML shell happily throughout the outage — a homepage browser check would have stayed green the entire incident. The failure lived in PostgREST; the check must reach PostgREST (`GET /rest/v1/catalog_items?…` with the anon key). |
| 2026-07-15 | **Defer the Splunk OTel Collector (AWS Lightsail) to Phase 3–4.** The free 15-host tier removes the cost objection but not the attention cost (a VM to patch, a Compose file, a scrape config that fails silently). The Collector would have shown a healthy DB during the outage — real value, but not the bottleneck. RUM + Synthetics delivered ~80% of the benefit with zero operational surface. Keep the design for when it's *wanted*, not *needed*. |
| 2026-07-15 | **Single Synthetics location (AWS N. Virginia).** Matches Supabase US East. Four locations = 4× requests and 4× false-positive surface for no added signal at this scale. |
| 2026-07-15 | **Watch Supabase legacy anon/service_role key deprecation.** Legacy keys are on a path toward Publishable/Secret keys. Not urgent — the app uses the legacy anon key and the Synthetics check matches it. Revisit when Supabase forces the migration. |
| Jun 11, 2026 | **Founder dashboard, not intranet.** Solo founder needs a launchpad/balance instrument; Drive stays source of truth for documents. |
| Jun 11, 2026 | **Four-lane charter structure.** Dashboard organized around standing functional lanes (charters), not links — so agents/hires onboard into a charter that exists before headcount. |
| Jun 11, 2026 | **Leverage ratio definition.** 1 human : tasks run by agents, summed from per-lane agent counts. Excludes C-suite "advice" Claude projects — keeps the number honest and un-inflatable. |
| Jun 11, 2026 | **Three-repo separation: `velayo-os` / `velayo-platform` / app repos.** Cockpit ≠ engine. Different audience, lifecycle, blast radius. OS never ships to a customer. |
| Jun 11, 2026 | **Automation-first hosting.** Cloudflare + Git push-to-deploy, not drag-and-drop. |
| Jun 11, 2026 | **Private = secure auth, not obscure URL.** Cloudflare Access OTP over in-page PIN (PIN is client-side theater). |
| Jun 11, 2026 | **Access policy: named emails + `@velayo.ai` domain rule.** Scales without per-hire edits. Coupling noted: issuing a velayo.ai email = granting Harbour access. |
| Jun 11, 2026 | **SESSION END pipeline is v2.** Chat emits `design_handoff.md`; Claude Code owns the canonical write. v1 Drive-writing Scribe retired. One log, repo is source of truth. |
| Jun 11, 2026 | **Single company log for now, scope-tagged.** Stays in `ourprovisions/docs` until app #2's first session — at that point split into `velayo-os/docs` as a filter, not a migration. |
| Jul 11, 2026 | **Product specs sort into `ourprovisions/docs/specs/{active,built,retired}/`; Velayo OS process docs route to `velayo-os/docs/`.** Two-repo split keeps the product decision-trail separate from OS/workflow machinery. Lifecycle folders created this pass (were design-intent only until now) — executed in `0303397`. |
| Jul 11, 2026 | **Diagnosis→fix spec pairs both go to `built/`, not retired.** Retired means design *changed*; a two-document trail of one shipped fix is forensic record, not supersession. |
| Jul 11, 2026 | **`CLAUDE.md` stays at ourprovisions repo root** (not `docs/`) — corrects a memory error; it is already correctly placed. |
| 2026-07-12 | **Catalog naming rule:** a household custom item may NOT duplicate a global item's normalized name (reuse the global); slightly-different names ("Wheat Bread" vs "Bread") are legitimate distinct customs. Matching is exact-on-**normalized** (lower + collapse-whitespace + trim), **NEVER fuzzy** — fuzzy would destructively merge distinct names. Shapes migration 018's reuse logic + 019's index predicates. |
| 2026-07-13 | **Bug 1 is a catalog-layer fork, not list-layer** — corrected by a prod query (3 list rows → 3 distinct `catalog_item_id`s). Fix at write-time (018 reuse) + DB backstop (019 indexes), NOT by scoping the list re-add. Reaffirms "diagnose against a live prod query, never against code comments or the initial hypothesis." |
| 2026-07-13 | **Global-vs-household collision deferral (from spec) resolved: F1c built.** Cross-scope shadowing (custom shadowing a global name) is enforced by 018's write-time reuse; same-scope self-forks are enforced by 019's two partial unique indexes. The two mechanisms cover the whole naming rule together — neither alone is sufficient. |
| 2026-07-13 | **F2 keying: kept check STATE name-keyed, made the WRITE row-specific (`.eq("id", listItemId)`).** Did NOT rekey state to `list_item.id` (Option B) — not justified by KISS now that 018/019 + cleanup make "two live rows, same name" structurally impossible. Revisit id-keying only if the F0 constraint ever fails to hold. |
| 2026-07-13 | **F3 Slow-3G display flicker (brief uncheck→recheck under throttling, self-corrects) logged as a known cosmetic edge — do NOT fix.** KISS: imperceptible in normal use; fixing risks destabilizing a proven optimistic guard (`pendingCheckRef`, mirror of `pendingQtyRef`). |
| 2026-07-14 | **Referral is a distinct primitive from household-invite.** Invite grants authorization (token → `household_members` row → write access); referral grants nothing (no token, no membership — points at the front door). Conflating them forces advocates to over-share or hand-explain a URL. Capture attribution at the link layer NOW (`?ref=<code>`) — referral chains can't be reconstructed retroactively; rewards logic deferred to Phase 4. |
| 2026-07-14 | **Chris's "own list" = a lens, not a fork.** A separate list forks the household and kills CI. A personal *view* of the shared list IS CI — shared truth through one person's lens at the moment of action. Consistent with the `activeHouseholdId` pattern (client lens; server checks membership). No per-user list. |
| 2026-07-14 | **Priority/intent signaling belongs on `list_items`, not the catalog and not per-user.** The point is Heddi's "skippable" is visible to *Chris* — it crosses the person boundary intentionally, like check and remove. Do NOT model it as a per-user catalog attribute like hide. Three states max; observed poles = "must-have" (shopper owns the miss) and "skippable" (requester owns the miss). Mechanism deferred to affordances/mockup work. |
| 2026-07-14 | **Multi-household member-adding: Option B (people-picker) approved; Option A (clone household) deferred.** Once two users share any household, adding one to another is *selection, not invitation* — the invite handshake exists to establish trust between strangers; re-running it between connected users asks them to re-prove what's proven. B touches membership only, degrades to invite-link for true strangers. A (clone carrying items/staples) is unvalidated — powerful for "same life, different location," harmful for a different context. If A is built, keep it composable with B, not monolithic. |
| 2026-07-14 | **Sub-decision on Option B — instant-add, but not silent.** Instant-add is fine between already-connected users, but the added user must *see* they were added, not be silently enrolled. Adding grants write access and exposes a household's existence + contents — "any user I share one household with can silently drop me into new households" has a consent edge. |
| 2026-07-14 | **Hidden-items findability is a correctness defect, not a design preference.** An unfindable hidden item is indistinguishable from a deleted one — collapsing the two-layer hide/delete distinction the model depends on. Hide must stay findable by the person who hid it. *(Delivered by F1b 2026-07-15 for the exact-match case; grouped-hidden-in-search is the residual.)* |
| 2026-07-14 | **Granular unhide is a byproduct of the search fix, not a separate feature.** Surfacing hidden matches in search with inline per-item unhide *is* the granular-unhide path; "unhide all" demotes to a wholesale convenience. No separate unhide-manager UI needed first. |
| 2026-07-14 | **Search/filter label: do not relabel standalone.** The "Search" bar behaves as a live filter (mis-priming the hidden-banana surprise), but once search surfaces hidden items it moves back toward genuine search. Decide label + behavior together in the hidden-items work, or risk relabeling twice. |
| 2026-07-14 | **Android keyboard real-estate: watch-item, not a fix.** Soft keyboard sandwiches the filtered list. Evidence thin (one platform, two users, no blocked task); moving the primary-screen search bar for a not-yet-painful problem — and bottom-anchoring collides with the top-search convention. Gather more data first. |
| 2026-07-14 | **Event as a second provisioning context — named strategic concept.** Household = standing/permanent; event = bounded/ad-hoc/deadline-driven (golf trip, bachelorette). The event may express the CI thesis *more* vividly (acute pain, unavoidable collaboration, natural referral surface, easier on-ramp). No change to Phases 1–3. |
| 2026-07-15 | **Hide is a lens, not an edit.** Re-adding an item that is hidden-for-me but live on the household list is un-hide only — reveal the shared row at whatever quantity the household set, write nothing. Rejected un-hide+increment and un-hide+set: no one changed the quantity, so the quantity must not change. A per-user view action cannot have a household-wide side effect. |
| 2026-07-15 | **A contributor badge is the last thing you said.** Attribution comes from utterance, not arithmetic. Your badge clears only when *you* (a) reduce by the full amount you asked for, or (b) reduce to zero. Everyone else's actions change the number, never your statement. Silence is not withdrawal. |
| 2026-07-15 | **No permission walls between household members.** Anyone may set the shared quantity to anything, including past their own contribution — no blocking, no warning. "We should not need software to protect people from other people in the house." Distinct from "shared list is sacred," which guards against *category* errors (a per-user lens masquerading as a household edit), not deliberate household edits. This is CI. |
| 2026-07-16 | **Referral = advocacy, and the real reason to build it is that hand-composed links are a defect surface.** Not "spread the word." Dan texted Heddi a schemeless `ourprovisions.velayo.ai`; her Android threw "Could not find an appropriate application." A link composed by the app (`https://` + prose, via Web Share) is well-formed **by construction**; a human at midnight is not. Owning the artifact is the fix — the same reason the household invite already carries a pre-written message. |
| 2026-07-16 | **Referral lives in Preferences, NOT in the household share sheet.** Different layers: invite is household-scoped, referral is app-scoped. Co-locating forces users to notice a distinction they don't care about at the exact moment we want the harder action — "just tell them about the app" is easier than "share my list," so it cannibalizes the depth metric (≥50% invite a second person; the invite is the ONLY path to the aha). |
| 2026-07-16 | **No referral gamification. Ever-or-later, not now.** Referral volume rewards blasting the link at everyone; one referred spouse produces the entire CI thesis, twenty referred acquaintances produce nothing. A leaderboard would rank the second above the first — contradicting the locked metric's warning that *breadth without depth is a false pass*. Confirms rewards→Phase 4 (2026-07-14). |
| 2026-07-16 | **Do not track referral sends — only successful signups.** `navigator.share` hands off to the OS and reports nothing: a send is **unobservable**; only *intent* (the tap) is knowable, and it overcounts every cancel. Conversions ÷ intents yields a rate that looks terrible and means nothing. Removes a table, a counter, and a misleading ratio. |
| 2026-07-16 | **Success = a `users` row exists with `referred_by` set; `referred_by` IS the metric.** The schema picks the honest definition — a `users` row only exists if they cleared Clerk **and** bootstrapped in. Counting Clerk accounts counts ghosts (Aidan made an account and never arrived). `count(*) where referred_by = <id>` — no counter to drift, no table to maintain; attribution and measurement are the same row. |
| 2026-07-16 | **Activation tracking is NOT a referral sub-feature.** "Did they log in / wrap a trip / share a household" is the **existing activation ladder** (R1–R5, 2026-07-09) — R2 *is* "invite a second person." Build activation instrumentation once for **all** users and slice it by `referred_by`; building it inside referral means building it again for organic users next month. (Caught live: scope grew from a share button to an analytics platform in six messages — what KISS exists to catch.) |
| 2026-07-16 | **The mission belongs on the landing page, not on a button.** "Help your friends live healthier, happier, earth-kind lives" is the reason **Dan builds**, not the reason **Glen shares** — delivered from a Preferences drawer for a grocery list, the gap between the ask (text a link) and the claim (improve a life) reads as marketing. The sine-wave page (Boardroom → Trough → Ah-ha → Vision → Front Door) carries the mission; the button only has to get them there. |
| 2026-07-16 | **Referral copy is the frame around the sharer's endorsement, not a pitch they forward.** Glen isn't relaying Velayo's words — the endorsement IS Glen; the link is the artifact. So the copy is plain and in a person's voice, and "we are building…" is wrong (it puts the company's words in the user's mouth). Superlatives ("best list app ever built") fight the positioning and invite argument; nobody forwards a boast. |
| 2026-07-16 | **Vocabulary is load-bearing: Harbour = the shared identity layer, Fleet = the apps, Crew = people on a trip. Users COME ABOARD.** "Join the fleet" invites your friend to become an app. Also: never use "invite" for referral — in this product *invite* is a real token-bearing grant, and two meanings is how users misunderstand what they accepted. |
| 2026-07-16 | **"Invite-arrival friction is systemic" is three distinct failures in three layers, not one defect.** Aidan = expectation (expected a download); Heddi = delivery (link artifact); Chris = app (join didn't activate). 3/3 is a true *pattern* but a false *diagnosis* — one fix cannot close all three, and designing as though it can ships a fix for Heddi and leaves Aidan untouched. |
| 2026-07-16 | **The Heddi RCS rich-card diagnosis (2026-07-14) is OVERTURNED.** Chris received a carded link on Android and it worked → card+tap is not sufficient to cause the failure. The original conclusion was reasoned from one screenshot with no control. **n=1 with uncontrolled variables is a story, not a diagnosis** — the governing rule applies to our own past conclusions, not just to stale mirrors. |
| 2026-07-16 | **The blocker is not invite-arrival friction — it is that there is no front door.** `beta_signups` is a table with no form in front of it and no email behind it. Two of three testers weren't given a broken path; they were given **no path** and handed a raw URL. Launch assets have been DESIGNED but UNWRITTEN since 2026-07-05/09, skipped across five build sessions. **Promote the sign-up page + questionnaire copy to NOW.** |
| 2026-07-16 | **Never reconstruct a missing migration file.** 009–012 and 017 aren't on disk; they ran and were never filed. A reconstructed migration is a guess wearing the costume of a record — worse than an honest gap, because the next session trusts it. If ever repaired, read `pg_proc.prosrc` from prod and stamp the header **"reconstructed from prod on <date>"**. Sequence gaps can't be reconstructed at all — a DB shows end state, not history. |
| 2026-07-16 | **A backup is not a backup until its TOC is verified and it is off-instance.** `pg_restore --list` must show TABLE DATA (not just schema); a dump on the same laptop is a copy, not a backup. `--no-owner --no-privileges` is correct for Supabase: platform role grants don't transplant, but RLS policies, constraints, RPCs, and realtime publications all survive. And: **a backup you must remember to take is one you will eventually forget to take** — tonight's dump is a floor, not a strategy; decide the cadence deliberately, not by drift. |
| 2026-07-16 | **Beta access is a TOLL, not a gate.** Completing the questionnaire *grants* access rather than requesting it. A gate makes Dan the rate limiter and promises personal onboarding his time can't scale past ~30 testers. The toll is honest about the exchange the spec already described ("willing to give to get"). Corollary: the page can no longer say "invite only" — if answering lets you in, nothing is gated. |
| 2026-07-16 | **The landing page is permanent; the beta door is temporary.** Design so the CTA swaps without rewriting the page. Two commented swap points: the **status slot** (`Beta` → `Available now` → `New version`) and the **CTA** (`Come aboard` → `Download`). Today's beta page becomes tomorrow's license page for free. |
| 2026-07-16 | **`ourprovisions.app` = story + door (static, public); `ourprovisions.velayo.ai` = the app (Clerk).** Separate Vercel projects, separate origins. Marketing at the app root means every request hits the React bundle before deciding what to show, and fights Clerk's redirect logic. Isolation also stops a marketing typo failing an app build under `CI=true`. |
| 2026-07-16 | **A question earns its place only if it changes a decision we're actually going to make.** Governing rule for the signup questionnaire; cut three of nine on it. Nice-to-knows are not free — they're the difference between a handshake and homework. |
| 2026-07-16 | **`shop_mode` (in-store / delivered / mixed) is a roadmap-forking question.** A service-shopper (Instacart / Fresh / pickup) is a household the roadmap doesn't serve as designed: Phase 2 smart list ordering is aisle-based and **there is no aisle**; Phase 3 cross-store pricing partly dissolves; Phase 3 receipt scanning gets *easier* (digital receipt). Worth knowing **before** building aisle-ordering for people who never walk an aisle. |
| 2026-07-16 | **One chip group, one axis.** Rejected merging store-count into `shop_mode` ("same store / different stores / service"): the first two answer *where*, the third answers *how*. Every combination exists, so single-select forces a true answer to be discarded and we'd never know which. Data that looks clean and isn't — worse than not asking. |
| 2026-07-16 | **Don't shape questions to fit an unbuilt table.** The pull to keep `store_count` populated came from the schema. The table doesn't exist; the questions are the thought, the schema is their shadow. |
| 2026-07-16 | **Say nothing about price until monetization is decided.** "In beta and free" was caught in a subordinate clause — that's how pricing commitments actually happen on landing pages, not in a pricing section but in an adjective nobody audited. "Free" is a promise users carry forward and resent losing. |
| 2026-07-16 | **The page demonstrates CI/AI/ExD; it never names them.** CI×AI×ExD is the internal decision framework, not customer copy — naming pillars turns a landing page into a pitch deck. The page shows CI (the shared list), *is* ExD (by being beautiful), and states AI as a consequence ("it remembers what you buy") rather than a badge. Strong brands don't name their pillars. |
| 2026-07-16 | **Don't sell fixed bugs as features.** Three captions independently contained bug-relief that only the builder can feel — "never a confusing '0'", "no copy-paste dance", "did it save?". The reader has never experienced the failure, so the brag teaches a defect and then congratulates us for fixing it. Recurring hazard when the person writing marketing is the person who fixed the bugs. |
| 2026-07-16 | **The shop is the pain; the list is the instrument.** Thesis frame, from Dan. Nobody wants a list — they want the trip to be over. Corollary: *"you don't need a budget to control what you spend on food. You need a list."* Buys the spending stakes without becoming a budgeting app (the impulse buy is what happens when you're wandering; budgeting is what you do after you've already overspent). |
| 2026-07-16 | **Copy must not assume a second person exists.** `just_me` is a crew chip and a real slice of households. The single onboarding instruction is conditional — "If you share a kitchen, invite them" — so a solo tester has a clean exit with no deficiency implied. A solo tester is still a real beta tester; they just don't hit the depth metric, which is *our* measurement problem, not theirs. |
| 2026-07-16 | **One instruction at the door, not a checklist.** A full first-run list (second household, invite, build a list, custom items, categories, swipe-to-edit, staples, filter) was rejected: wrong surface (they're about to cross an origin and an OTP), it turns the toll's reward into homework, and it contradicts the ★ invisible-affordances finding — whose conclusion was *one reusable milestone-keyed coachmark primitive in the app*, not per-feature written instructions. Kept the invite because it's the CI activation metric. |
| 2026-07-16 | **Form declares function — one shape, one meaning.** The status slot and the nav CTA had both become sand outline pills: identical objects, one inert and one pressable. Each decision was defensible alone (quiet the button; contain the short word); together they made the vocabulary lie. Status is a *state*, not an object — container stripped. On this page a pill now means exactly one thing: press me. Argues for eye-testing the whole, not the parts. |
| 2026-07-16 | **CTA prominence tracks whether another CTA is visible.** The nav "Come aboard" stays a ghost while the hero's own button is on screen (two loud buttons thirty pixels apart is one too many) and solidifies once it scrolls away. Corollary: **"Come aboard" invites; "Welcome aboard" receives** — Come = the ask, on every button; Welcome = the greeting, on the confirmation, once. |
| 2026-07-16 | **A Velayo-scoped table (`beta_signups`) lives in the app-scoped Supabase project.** Pragmatic per KISS. Recorded so it reads as a choice, not an accident. |
| 2026-07-15 | **Badge count may exceed what the quantity can satisfy** (two people badged on one apple). A household conversation, not a data error. Rejected a special-case rule at qty 1 — it introduced a discontinuity ("why 1 and not 2?") and contradicted "only your own action changes your badge." |
| 2026-07-15 | **`added_by` is dead as a display source.** A scalar that can only name the creator, is never updated after INSERT, and goes stale the moment anyone else touches the row. The `list_item_contributors` ledger is canonical for attribution. Shipped the display-half fix (Shop name-line). Remaining reader: the remove-confirm dialog — repoint, then drop or demote `added_by` to audit-only. |
| 2026-07-15 | **Clear affordance debt at the point of discovery** (the Add-pill drift) rather than deferring it to a queued design session, when the answer is obvious and the pattern is already established. Deferral is for decisions, not stragglers. |
| 2026-07-15 | **Don't buy infrastructure during an outage.** Backups are a real gap (Free tier, no recovery path, real beta users' data) but the Pro decision was deferred to a green dashboard — you can't verify an upgrade took while the platform is down, and reacting to an incident is how you over-provision. Nano is not why prod was down; Supabase's platform incident was. Zero-cost `pg_dump` floor first. |
| June 8, 2026 | **Catalog visibility model.** Global seed list = permanent, undeletable household-wide, exists to get households started. Custom items = any member can add/delete, household-wide. **Hide** = per-user, browse-only, reversible, never touches the shared list. **Delete** = custom items only, household-wide, cascades. Global reset = separate, gated, re-seeds. Multi-item hide deferred until demand. Two principles: (1) the shared list is sacred — no per-user view preference suppresses it; (2) every removal has a proportionate undo (hide → unhide one-tap personal; custom delete → soft-delete recovery window; reset → deliberate and total). |
| Jun 12, 2026 | **Dev isolation = second free Supabase project, not branching.** Branching is Pro-only and metered; a second free project is $0 and gives hard isolation. Trade-off accepted: schema changes don't auto-merge — applied via files by hand, matching existing migration discipline. |
| Jun 12, 2026 | **`bootstrap_new_user` canonical = 4-arg form** `(p_clerk_id, p_email, p_invite_code, p_full_name)`. Prod has four overloads (ambiguity risk); dev installs only this one. |
| Jun 12, 2026 | **Category persistence works as designed — revisit only on user friction.** Categories are item-derived; an empty category disappears when its last item is deleted. Considered promoting to first-class persisted entities; deferred. Edge case affects only the creator of an empty category, not the common case. |
| Jun 12, 2026 | **`.env.local` now points at dev DB.** Was silently pinning local to PROD (CRA precedence: `.env.local` overrides `.env`). Root cause of repeated "test hits prod" confusion. `.env.local` is gitignored. |
| Jun 12, 2026 | **Single canonical baseline over renumbered ordered set.** One `000_canonical_baseline.sql` rebuilds prod from empty. Goal is reproducible truth validated by a clean rebuild — a single file makes the diff unambiguous. Evolutionary history stays in `archive/` + SESSION_LOG. |
| Jun 12, 2026 | **Reproduce known debt AS-IS; never fold fixes into the baseline.** The baseline must match what's actually running, bugs included. Fixes go in separate, named, tested migrations so validation isn't muddied. Debt flagged inline with `-- KNOWN DEBT`. |
| Jun 29, 2026 | **RLS policy fixes use `is_member_of` where the check is pure household membership, but `get_current_user_id()` where ownership or crew-keying applies.** The two `sessions_*_own` policies enforce *owner-is-you* (not just membership) — `is_member_of` alone would let any member touch another's session, so they keep `get_current_user_id()`. The two crew policies are crew-keyed (no `household_id`), so `is_member_of` doesn't apply — also `get_current_user_id()`. Deliberate per-policy carve-out, not an oversight. |
| Jun 29, 2026 | **Helper-consolidation survivor choice is security-driven, not arbitrary.** Kept `get_current_household_id` / `get_current_user_id` because they pin `search_path` (a real SECURITY DEFINER property) and are deterministic (`order by joined_at desc`); dropped the two NULL-config variants. |
| Jun 29, 2026 | **This project has NO Supabase migration tracker** (`supabase_migrations.schema_migrations` does not exist on prod). Migrations are applied by hand in the SQL editor, never via CLI. Consequence: filing numbers (e.g. 014/015) are a folder convention, not load-bearing; "truth is a query against the correct environment" is the only authority on applied state. Migrations must be idempotent and verified against the live catalog. |
| Jun 29, 2026 | **`.app` domain wiring parked** pending domain-ownership consolidation. Blocker: split ownership — `ourprovisions.velayo.ai`'s parent is under `daniel.l.holmes@gmail.com`, `ourprovisions.app` under `dan@velayo.ai`. Fix (queued): transfer all domains to `dan@velayo.ai`, stand up a `domains@velayo.ai` role alias, THEN wire DNS/Vercel/Clerk. Single-owner, role-based domain admin. |
| Jun 29, 2026 | **`BUILD` is a real command (Claude Code, in `CLAUDE.md`); `SPEC` is NOT a trigger.** BUILD compresses a six-step routine (locate spec in airlock → grep-before-edit → one scoped commit → test on deployed dev → stop at dev → leave for SESSION END), so it earns being a command. SPEC does not — the design chat produces specs by judgment, not on a keyword; `SPEC` is retained only as a manual **override** ("write the full spec even though you'd otherwise give a plain instruction"). *(Supersedes the earlier "two-verb workflow" framing.)* |
| Jun 29, 2026 | **Spec discipline — fewer artifacts.** Produce a `SPEC_*.md` only when a change carries a decision, a risk, or a verification need (migrations, schema, prod-touching, non-obvious "why", truth table, multi-step). For obvious single-edit changes, hand Claude Code a plain instruction instead. Test: if the spec would mostly restate the diff, skip it — `docs/` holds decisions, not every keystroke. (Prompted by this session producing ~7 specs, of which the one-line pointerEvents fix was ceremony exceeding the work.) Added to the design-chat project instructions. |
| Jul 1, 2026 | **Attribution names are sourced from `users.full_name` (DB), reconciled from Clerk client-side each session via a dedicated effect — NOT from `bootstrap_new_user`.** Bootstrap runs once, too early (before Clerk name ready), and can't re-run; a decoupled idempotent effect (Effect 1b) is self-healing and sidesteps the RPC's 4-overload ambiguity. |
| Jul 1, 2026 | **Name fallback philosophy: real name → composed Clerk `firstName`+`lastName` → email prefix → generic.** Never fabricate a name from an unstable cache. Applied in both the reconciliation effect and the profile-sheet heading. |
| Jul 1, 2026 | **Diagnose against a live prod query, never against code comments or docs** (reaffirmed, cost 5 wrong hypotheses this session). Comments claimed migration 009 cleaned contributors and that the newest RPC saved `full_name` — both true and both irrelevant; the real bug was upstream (NULL `full_name`). |
| Jul 1, 2026 | **The lens (`ActiveHouseholdContext`) is the single writer of the active household.** Every join path lands the active household through `switchHousehold`, never by calling `setHousehold` directly. The dead `acceptInvite` (which swaps `useProvisions.household` directly and would bypass the lens) is the anti-pattern; a future paste-a-code UI must route through the lens like the switcher. Prevents a split-brain where `household` and `activeHouseholdId` disagree. |
| Jul 1, 2026 | **Switch intent must outlive the single invite-consuming load.** The invite is single-use and `?invite=` is stripped on the consuming load, so any one-shot switch tied to sessionStorage flags has no recovery if interrupted. Intent is held in React state (`pendingJoinId`) and fired reactively when the joined household appears in `myHouseholds` — decoupled from both the stripped flags and localStorage-write timing. First instance of the durable-intent-over-one-shot pattern. |
| Jul 1, 2026 | **Text size is device-local, never account-synced.** It is a property of the screen and the light, not the person — the boat tablet wants XXL, a phone may not. Persisted in `localStorage` (`op_list_text_size`, index 0–4), never Supabase; the control lives in Preferences (set ~once per device), not on the list screen. |
| Jun 12, 2026 | **Drop the 3 dead `bootstrap_new_user` overloads; keep only the 4-arg version.** Overloads are an ambiguity landmine (PostgREST could resolve the wrong one). Confirmed against `useProvisions.js`. |
| Jun 12, 2026 | **Live `close_cycle` is canonical, not the 005 file version.** Deployed function (upsert on `(household_id, catalog_item_id)` + pre-upsert badge clear) supersedes the plain-insert version in the 005 file. Prod wins over files. |
| Jun 12, 2026 | **Don't seed the 10 duplicate catalog items in the baseline.** A clean baseline seeds clean (38 items). The duplicates remain a prod-only fixture for the future catalog-merge feature; add manually in dev if needed. |
| Jun 12, 2026 | **`migrations/` is the permanent schema home.** Schema files previously lived only in Project knowledge. `migrations/` + `migrations/archive/` + `README.md` are now committed so every future schema change has an obvious, tracked location. |
| Jun 13, 2026 | **Dev environment is reproducible-from-git, not synced.** Any machine rebuilds from clone + `.env.local` + `npm install`. Same discipline as prod rebuilding from `000_canonical_baseline.sql`. Nothing precious lives on one device. |
| Jun 13, 2026 | **Node pinned to major 24** via `.nvmrc` + `package.json engines`. Pin the major only — Vercel guarantees major version and auto-rolls minor/patch, so over-pinning a patch would drift from CI. |
| Jun 13, 2026 | **`legacy-peer-deps=true` committed in `.npmrc`.** `react-scripts` 5.0.1 + React 19 trips fresh `npm install` with ERESOLVE; sticky flag keeps every machine + Vercel consistent so plain `npm install` works. |
| Jun 13, 2026 | **Secrets interim = Google Drive file (My Drive, unshared); planned = Bitwarden.** Acceptable stopgap because `.env.local` holds anon/publishable keys only (RLS is the real lock). `vercel env pull` is NOT a safe distribution route until Development-scope vars are repointed to dev. |
| Jun 16, 2026 | **SHOP swipe acts on the list, not the catalog.** Swipe was calling `hideItem` (catalog, per-user, browse-only) — wrong layer for a shopping gesture. Split: SHOP swipe = remove `list_item` (`deleted_at`); Hide stays a BROWSE/catalog verb. "Shared list is sacred" preserved — removal is an explicit list action, never a silent side effect of browsing. |
| Jun 16, 2026 | **"Not buying this trip" needs no gesture.** Leaving an item unchecked already means "still pending." SHOP swipe has exactly one destructive meaning (remove from shared list); Cancel on the shared-item modal is the "keep it for someone else" path. No third branch. |
| Jun 16, 2026 | **Confirm modal for shared items, instant remove for own.** Own item = sole contributor = no one else affected → remove immediately. Any other contributor present → confirm modal naming the adder. Chose modal over undo-toast: the action crosses a person boundary, so explicit Cancel beats a silent timer. |
| Jun 16, 2026 | **Ownership decision lives at the swipe site (App.js), not in SwipeToRemove.** SHOP `SwipeToRemove` is full-swipe-commits — row animates off before `onRemove` fires ~400ms later. Branching own-vs-shared in `handleSwipeRemove` avoids a confirm modal fighting a committed animation. Cancel springs the row back cleanly because `listRows` is never mutated on Cancel. |
| Jun 16, 2026 | **Stop keying item actions on name; resolve by `catalog_item_id`.** The "not in catalog" bug was the same name-key footgun as the multi-session sync bug chain. `toggleChecked` and `removeFromList` now resolve the stable id from `listRows`. Name remains display and optimistic-state key only. |
| Jun 16, 2026 | **Root cause of dev block was grants, not RLS.** "Permission denied for table" = role lacks table GRANT (fails before RLS is consulted), distinct from an RLS policy returning zero rows. "Reset Public Schema Permissions" query stripped `authenticated`/`anon`. Diagnostic: `information_schema.role_table_grants`. Fix goes in a standalone dev-only migration, never folded into `000`. |
| Jun 16, 2026 | **Velayo project template is dual-mode (DESIGN vs HANDOFF), switched by one MODE line.** Rationale: new apps begin repo-less (chat must scribe directly); a single-mode template is wrong half the time. Matches the "split at app #2's first session" principle. |
| Jun 16, 2026 | **Repo `docs/` is canonical; Project Knowledge is a mirror, not source of truth.** Rationale: the v2 flow made Claude Code the merge authority — the old "re-upload Project Knowledge" model inverted ownership. |
| Jun 16, 2026 | **Template `[SCOPE]` uses `[APP NAME]` placeholder, not literal "OurProvisions".** Rationale: prevents every new app inheriting OurProvisions' scope vocabulary. |
| Jun 16, 2026 | **Multi-household is the LAST structural feature before the AI phase.** Ship it clean before store awareness / receipt parser. Rationale: AI features are more valuable with multiple household contexts to learn from. |
| Jun 16, 2026 | **Two roles only (owner/member in DB). No permission-variant creep.** Owner gets destructive/structural actions (rename, remove member, delete household); all list actions shared. Succession to oldest member on owner departure; no co-owners. |
| Jun 16, 2026 | **Role vocabulary stays out of the UI.** Internal shorthand "captain/crew"; DB stores owner/member; UI shows capability (a Remove button), not role words or badges. Avoids "seafood restaurant" theming. |
| Jun 16, 2026 | **Switcher reveals progressively: no switcher at 1 household, household-name sub-line appears at 2+.** "Create new household" is the act that unlocks the switcher. Zero new chrome until the feature is needed. |
| Jun 16, 2026 | **Store awareness sequenced as its own arc AFTER multi-household — not interleaved.** Don't-stack discipline. |
| Jun 16, 2026 | **Store awareness target model: silent GPS auto-detect (Scenario D from migration 005).** If location shared, auto-match to known_stores; manual define otherwise. N3-style price steering ("cheaper at Market Basket") is the north-star payoff — build only once price-history is dense enough to be honest. |
| Jun 17, 2026 | **Multi-household reads go through `get_my_households()` SECURITY DEFINER RPC, not direct table queries.** `household_members` SELECT policy is scoped to the active household — a direct query cannot enumerate other memberships. Identity resolved internally from JWT (no user-id param). |
| Jun 17, 2026 | **`repo migrations/` is the SINGLE SOURCE OF TRUTH for migrations.** Numbering resets after the 000 canonical baseline → new migrations are 001 onward. Google Drive SQL copies are stale/pre-baseline, NOT authoritative. New migrations born in repo via Claude Code, tested on dev, then applied to prod. |
| Jun 17, 2026 | **`bootstrap_new_user` DESC ordering (migration 002) is an EXPLICIT TEMPORARY STOPGAP.** The real fix is to make bootstrap load the context's active household. Do not treat 002 as done. |
| Jun 17, 2026 | **Future `create_household` flow must be a single SECURITY DEFINER RPC.** `households.created_by` is NOT NULL — two separate client writes (insert household, then insert membership) could half-complete. Wrap both in one atomic transaction server-side. |
| Jun 17, 2026 | **Active context is client-authoritative — the Harbour standard.** Each app instance holds its own active household (context + localStorage); the server authorizes the claimed household, never picks one. Chosen over a server-global column (forces cross-device lockstep, an explicit non-goal; forbids desired per-app divergence). Generalizes to the fleet (active vessel, active kitchen). |
| Jun 17, 2026 | **`is_member_of(household_id)` is the shared authorization primitive (migration 003).** One SECURITY DEFINER boolean: "is the caller a member of this household?" Fails closed on null. Write policies authorize through it instead of comparing against a single guessed household. Proven on dev (true/true/false/false). |
| Jun 17, 2026 | **Migration 004 scoped to `list_items` only — deliberately.** The same `= get_current_household_id()` write-gate exists on five other tables (same latent bug, dormant). Convert the hot path, prove it, take the rest as a separate reviewed arc (future migration 005). Don't-stack. |
| Jun 17, 2026 | **UPDATE policies get a `with check`, not just `using`.** Membership must gate both which rows you touch AND which household you can move a row into. Hardening the originals lacked. |
| Jun 17, 2026 | **Layered default-household rule.** device-last (localStorage) → home household (deterministic, replaces 002 stopgap) → future confident-GPS one-tap confirm. Human always commits the active context. |
| Jun 17, 2026 | **Switcher is gated on a `useProvisions` re-scope, not just UI.** `useProvisions` must consume `activeHouseholdId` from `ActiveHouseholdContext` as its single household source before any switcher sheet is meaningful; the sheet alone would update context and change nothing visible. |
| Jun 17, 2026 | **Wordmark encodes shared-vs-solo per active household.** "OurProvisions" when the active household has 2+ members; "Provisions" when solo. Falls out of `householdMembers.length`, which Effect 2 reloads per active household — no special logic needed. |
| Jun 17, 2026 | **One unified "manage household" sheet.** Not separate switcher + members sheets: switch households at top, manage members/rename/invite of the active household below. Tapping at top re-scopes the app and the member section updates live. Single coherent "manage household" mental model. |
| Jun 17, 2026 | **Title-bar tappability decoupled from member count.** Sheet opens whenever signed in (even a solo single-household user can create a second household). Only the wordmark TEXT stays member-count based. |
| Jun 17, 2026 | **Create-new-household closes the sheet and lands on the new empty list with a toast (option B, fewer clicks)** rather than staying open or requiring a separate navigation step. |
| Jun 17, 2026 | **`households` authorization converted to `is_member_of` (migration 005).** A user may read/update any household they belong to, not just the active-heuristic one. `with check` added to UPDATE (the original lacked it). Invite-preview subquery on SELECT preserved verbatim. Unblocked the 406 on Effect 2's household fetch. |
| Jun 17, 2026 | **Owner-only enforcement on `households` UPDATE/DELETE deliberately deferred.** Migration 005 gates on membership as the correct authorization FLOOR; owner-vs-member is enforced in UI today. DB-level ownership gate ships alongside `delete_household` next session. |
| 2026-06-18 | **Contributor 403 root cause was `household_members_select`, not the contributor policy.** A SELECT policy gated on the single guessed household blinds every inline membership join. Fix: authorize `household_members` SELECT via `is_member_of` — the contributor policy needed no change. |
| 2026-06-18 | **Use SECURITY DEFINER `is_member_of` (not inline subquery) on `household_members_select`.** Inline subquery against the same table re-triggers the policy (RLS recursion). SECURITY DEFINER bypasses RLS inside the function body. Recursion-safety rule: any policy that must read the table it guards must route through a SECURITY DEFINER helper. |
| 2026-06-18 | **Migration 007 completes the authorize-don't-guess sweep.** Remaining five `get_current_household_id()` gates (household_members select, waste_events, catalog_items select+insert, household_invites insert) converted to `is_member_of`. `get_current_household_id()` is now retired from all active write/read policy paths. |
| 2026-06-18 | **Prod bundles get one outer transaction; strip inner `begin/commit` from member migrations.** Migration 005's standalone transaction control would defeat the bundle's rollback protection, dropping 006+007 outside the transaction. Fix in the derived bundle, leave source migrations standalone-correct. |
| 2026-06-18 | **Ship DB ahead of code, deliberately.** New policies are strictly more permissive in the correct direction; old single-household frontend operates safely over them. De-risks a large feature: migrate + prove-harmless first, then merge the frontend. |
| 2026-06-18 | **`dev`→`main` merge is the multi-household go-live event, not a ride-along.** It deploys the switcher to all prod users on push. Treat as its own deliberate session with the live-now-vs-after-owner-gate product decision made up front. |
| 2026-06-19 | **Invite-join switching is intent-gated on user state.** First household ever → auto-switch (nothing to interrupt; invite is the front door). Established user joining another → silent join (don't yank an in-flight user; new household appears in switcher, available on tap). Extends "user holds the lens / confirmation not silent switch" from location to membership. Established-user-with-untouched-list auto-switch was specified but NOT built (requires a pre-join authored-state snapshot because Effect 2 wipes prior-household maps before the banner fires); `hadPrior`-only gate shipped instead. Rationale: silent join fails safe (one tap) vs. mid-work context loss; the edge case is rare and unmeasured, so deferred pending usage evidence. |
| 2026-06-19 | **Resolver is single-source: Effect 2 follows `activeHouseholdId`, full stop.** Removed `justJoinedViaInviteRef` forced-fallback. In auto-switch, `switchHousehold` already sets `activeHouseholdId` to the joined household before Effect 2 resolves; in silent join, it stays on the prior household. Trusting `activeHouseholdId` is correct in both cases. The forced-fallback was the third instance of "multiple paths each decide which household to load, and disagree." |
| 2026-06-20 | **Use column-target ON CONFLICT (household_id, catalog_item_id), not ON CONFLICT ON CONSTRAINT name.** Dev and prod have drifted on the unique-constraint name (dev: auto-named; prod: `list_items_household_catalog_unique`). Column form resolves to whichever covering unique index exists — one migration file applies cleanly to both environments. |
| 2026-06-20 | **insert_list_item upsert uses last-write-wins on quantity, not additive.** Additive would fight the `updateQty` UPDATE path (absolute set-value semantics); two competing semantics would cause drift. Concurrent-add race is rare enough that LWW is the safe, consistent choice. |
| 2026-06-20 | **On conflict, clear deleted_at and force status='pending'.** Concurrent add against a soft-deleted slot resurrects cleanly instead of erroring. Eliminates the Lemons 409 scenario as a side-effect of the concurrent-add fix. |
| 2026-06-22 | **Activator principle.** The person who creates a space carries NO ongoing authority over the group — only the structural responsibility of being the un-removable anchor. Equal authority within the group is foundational to "Our*" products; the moment one person retains ongoing control it becomes a different product. |
| 2026-06-22 | **Attribution vs. ownership rule (Harbour-wide).** Attribution is free and universal (who created an item/category/household, shown plainly). Ownership is reserved ONLY for irreversible, group-affecting actions (delete household) and pinned to a single consistent anchor. Items/categories carry attribution; they carry no ownership anchor. |
| 2026-06-22 | **Membership model = text-group model.** ANY member can add anyone and remove ANY non-creator (incl. self = leave). Creator cannot be removed by anyone and cannot leave — must delete the household to exit. Overrides the earlier "remove is owner-only" brief. |
| 2026-06-22 | **Keep `role = 'owner'` in the schema (do NOT rename to `creator`).** The seat is expected to accrue real responsibility later; `owner` is the honest name for "anchor that runs flat today, may carry weight tomorrow." Renaming to `creator` would mislead. Philosophy lives in BEHAVIOR (flat) + UI ("Created by …"), not the column name. |
| 2026-06-22 | **Owner exit is delete-only in phase 1.** No ownership transfer — a replacement owner creates a new household. Add transfer only if real demand surfaces. |
| 2026-06-22 | **Removed-user notification uses 30s membership-presence check, NOT realtime.** Live RLS (`is_member_of` filters `deleted_at`) suppresses the self-removal broadcast to the removed user. ~30s latency accepted under KISS; avoids touching the highest-stakes RLS surface. General pattern: any realtime-on-soft-delete using an `is_member_of`-style policy hits this suppression. |
| 2026-06-22 | **On removal from one's ONLY household, auto-provision a fresh empty "My Household."** Reuse `create_household`; in-flight guard against duplicate spawn. Never show an empty void or dead-end. Notice line 2 explains the fresh list. |
| 2026-06-22 | **Removed vs. voluntary-leave disambiguated by local `selfDepartureRef`, not a server signal.** Set in `handleLeaveHousehold` before the RPC fires. Fail-safe: explain-on-uncertainty (never silent-unexplained-removal). |
| 2026-06-22 | **Notice is built channel-ready (typed message shape) but single-tenant.** Queueing, priority, and migrating `showToast`/`joinBanner`/connectivity events DEFERRED until a 2nd real tenant exists. |
| 2026-06-22 | **No owner/creator status pill on member rows.** Attribution lives on the household header only ("Created by {name}" / "Created by you"). The creator's row is simply the one without a remove control. No explanatory text for the absent Leave button (don't explain a thing that isn't there). |
| 2026-06-20 | **Classify transport failures separately from HTTP errors (classifyFetchError pattern).** Transport failures (no HTTP response) → pill + keep last-good. HTTP/RLS/Supabase errors → red toast unchanged. Default `'real'` when uncertain — better a loud real error than silently swallowing one. |
| 2026-06-20 | **Rollback optimistic writes unconditionally on any error — transient or real.** Rollback runs first; then branch the notification. User always returns to true-server-state on failure regardless of failure type. No phantom saves. |
| 2026-06-20 | **Quantity zero = off the list (hard reset), not paused.** Re-add credits only the re-adder. Badge is present-tense ("who wants this now"), never historical. Zero-out path must atomically clear contributors (migration 009) or old badges resurrect from the tombstone on re-add (confirmed bug). |
| 2026-06-20 | **Membership exit (Leave/Remove) gated on cycle-boundary question.** If provision_cycles are user-facing → ship with "applies at next boundary." If still backend-only → ship simpler rule first; design cycle-boundary participation when cycles surface. Don't stack a dependency on an unloaded seam. Owner cannot Leave — must transfer or delete. Items stay on list (shared list is sacred). Attribution badge clears on exit (display rule only; added_by is never nulled). |
| 2026-06-24 | **Transient guard must distinguish "fetch failed, empty" from "succeeded, genuinely empty."** The marine-wifi guard conflated both. Narrowed `checkPresence` guard to `error \|\| !data` — a clean empty array (`{error:null, data:[]}`) only ever means a successful zero-row result and is a legitimate removal signal (user removed from their only household). Removing `data.length === 0` does not reopen the false-removal hole because genuine transient failures always surface as `error` truthy or `data` null. |
| 2026-06-24 | **One self-departure path only.** The own-row trashcan (self-`remove_member`) was a redundant second leave path that double-fired the legacy toast AND the variant-B rectangle, and bypassed `selfDepartureRef`. Hidden via `!isMe &&` guard reusing the existing YOU-badge condition. LEAVE HOUSEHOLD button is the canonical and sole self-departure path. |
| 2026-06-24 | **Removal-notice fallback wording: "that household," not "your household."** Avoids implying ownership of something the user may never have owned, when the real household name can't resolve. |
| 2026-06-24 | **Part B (`selfDepartureRef` slow-network wiring) deferred, scaffolded.** KISS — build only if a real slow-network voluntary-leave double-message is observed in production use. The refs are in place; the check is a TODO comment in `checkPresence`. |
| 2026-06-24 | **Hide the DELETE HOUSEHOLD button before the dev→main merge.** Delete-household is still a `console.log` stub; shipping an inert destructive button is a broken window. One-line conditional hide → merge all working multi-household value now → un-hide when delete-household is built. |
| 2026-06-24 | **Dev test-environment pollution blocks valid multi-user testing.** Junk households + lookalike `+testN` accounts caused four separate false test signals (Berlin pointer-drift, duplicate Test333, junk My Households, wrong-window observation). Environment cleanup is a prerequisite for further multi-user validation — not optional. |
| 2026-06-25 | **Bug 1 ("that household" wording) closed as test-environment artifact, not a code defect.** A clean, DB-verified single-household removal rendered the real household name correctly via `activeHouseholdNameRef`. Retroactively validates the Fix-2 sticky-ref design from 2026-06-24. |
| 2026-06-25 | **Auto-provision's real-world trigger is invite-only users.** Owners cannot leave their auto-created My Household (Leave is blocked on owner role; Delete is a stub). The only-household-removal case in production fires specifically for invite-first users, who by construction never have a personal household. Resolves the open design question about when point 4 actually fires. |
| 2026-06-25 | **Join-banner → `systemMessage` channel migration is promotable to NEXT.** SPEC §6 deferred this until a 2nd real tenant existed; the removal notice is now a shipped tenant, so the precondition is met. Migration owns the deferred queue/priority decision — must be its own session, NOT bundled into the dev→main merge. |
| 2026-06-25 | **Prod state is established only by a query against the prod tab, never by the log.** Today's drift survived because "applied to prod / verified present" was self-reported (likely run against the dev tab). The DB is the source of truth; the doc records what the query showed. No "applied to prod ✅" without the prod-tab result attached. |
| 2026-06-25 | **Adopt a pre-merge prod gate.** Before any dev→main: run the read-only RPC-presence + policy check (harness Part A) on prod. Any missing RPC the deployed frontend calls = block the merge. A green Vercel build says nothing about the DB; frontend and migrations ship on separate tracks. This gate would have caught today's bug pre-merge. See `qa/agent_test_harness.md`. |
| 2026-06-25 | **Test is event-triggered, not a SESSION END sub-step.** Pre-merge gate fires on dev→main; destructive suite fires after a dev migration; static checks on commit. SESSION END/handoff records Test results but does not invoke Test. Avoids firing tests on no-op sessions; ensures the merge gate blocks at merge time. |
| 2026-06-25 | **Adopt Supabase CLI migration workflow (staged).** CLI's `schema_migrations` table makes prod self-describing; `db push` applies only the gap. Current DBs are hand-built/drifted. Sequence: (1) reconcile migrations folder; (2) `db pull` baseline + `migration repair` to mark history on dev+prod; (3) link both, switch to migration-files-only. Prerequisite: secrets hygiene (Bitwarden). Prod `db push` stays human-triggered — no auto-rollback on prod. |
| 2026-06-25 | **Target agentic-QA pipeline (staged build order).** Stage 0: deterministic gate (the harness). Stage 1: automate verifiable file copies (handoff→Claude Code, docs→project) — only once the gate can catch a corrupted result. Stage 2: automate the QA run (report failures to human, don't auto-fix). Stage 3: guarded QA↔Claude Code autonomous fix loop — agents edit app code but NOT the tests; each fix a reviewable commit; bounded retries then escalate. Stage 4: handoff/Scribe records pipeline provenance. Principle: automate deterministic toil first, judgment last; keep a human at the prod (irreversible) boundary. |
| 2026-06-26 | **`repo/handoff/` is an AIRLOCK, not a doc home.** Permanent residents: `.gitignore` + `DESIGN_CHAT_handoff_prompt.md`. `design_handoff.md` = reserved merge-and-delete. All other files = payload, filed to their home (specs → `docs/`) and cleared each SESSION END. `## DROPPED_FILES` manifest in each handoff declares destinations. Rationale: lets design chat hand off everything it produces in one predictable place, while keeping the merge-and-delete logic unambiguous (only ONE filename is magic; baseline two are sacred; the rest is routed by manifest). |
| 2026-06-26 | **Catalog carry-forward = clone-forward (snapshot at creation), NOT a persistent fleet catalog layer.** Three-tier `is_global` model solves an unfelt sync pain and destabilizes the binary discriminator driving Hide/Delete verbs. Clone-forward fully solves the felt pain (rebuilding categories). KISS — "wait until people complain." |
| 2026-06-26 | **Clone scope = custom catalog ONLY.** Never clone `list_items`, waste events, cycles, sessions, or prices. New household opens with an empty shopping list. Lists are situational and independent; carrying them forward would break "the shared list is sacred" and confuse a fresh household with stale state. |
| 2026-06-26 | **Source household is user-chosen; default = most-recently-active.** Users expect their catalog to carry forward; "Standard provisions" (no custom items, null source) is the deliberate opt-out. |
| 2026-06-26 | **`create_household_from_template` WRAPS `create_household` (006), not modifies it.** The proven, prod-live empty-household path is preserved byte-for-byte; clone logic is purely additive; null source = identical passthrough. Two distinct RPCs, one delegates to the other. |
| 2026-06-26 | **Delete-household is soft-delete cascade, owner-only, reusing Layer-2 switch/provision.** Soft-delete sidesteps all NO ACTION FKs (nothing physically removed → nothing orphaned; cascade order is forgiving). To a surviving member, household deletion is observationally identical to removal — the existing 30s checkPresence + removal notice + switch/auto-provision path already handles it. Genuinely new code is only the RPC and the confirm-gated button. |
| 2026-06-26 | **D7 — catalog loss on delete: warn now, rescue later.** Honest in-confirm custom-item count warning ships with the delete feature. The clone-first escape hatch (a "Clone catalog first" button alongside "Delete anyway") is deferred to the clone-forward build. Delete and clone-forward are siblings; marker comment left at the confirm site. |
| 2026-06-26 | **One provisioning path only — resolveAfterHouseholdLoss owns the guarded switch-or-provision flow.** checkPresence and handleDeleteHousehold share a single useCallback in ActiveHouseholdContext that holds the provisioningRef guard, refreshes the authoritative list (no stale-closure reads), and either switches to a survivor or auto-provisions. A hand-rolled second path in the delete handler risks duplicate "My Household" spawns. |
| 2026-06-25 | **Secrets hygiene (Bitwarden) promoted to BLOCKER for agentic automation.** Agents need credentials to run DB tests or push to prod. Google Drive `.env` stopgap is acceptable for the current human-only flow; unacceptable once agents need access. Bitwarden is now a critical-path prerequisite for every automation stage past Stage 0. |
| 2026-06-28 | **Active-household indicator lives in the outer App shell, not the per-household chrome.** Outer chrome (avatar, kebab menu row) is always rendered regardless of household context; per-household chrome (title bar, wordmark, action buttons) is contextual. Indicator that survives household switches belongs in the outer shell. Header layering principle: outer chrome = identity/navigation layer; inner chrome = household-context layer. |
| 2026-06-28 | **Phase I/II indicator split: outer chrome now, harbour identity lens later.** Phase I ships a per-app (OurProvisions) indicator in the outer banner — minimal, ship-now. Phase II is a cross-app shared active-household lens at the harbour/Velayo OS identity layer. Two different scopes; deliberately separated so Phase I doesn't block on `velayo-os` maturity. |
| 2026-06-28 | **Indicator form: anchor icon + plain household name, tap-to-manage.** Anchor glyph signals "this is home base, you can navigate from here." Plain name (no quotes, no role label) is how members already refer to the household. Tap opens manage-household modal — no new navigation pattern, no new surface. |
| 2026-06-28 | **People glyph + "TAP TO MANAGE HOUSEHOLD" subline retired when Phase I indicator ships.** Both are crude substitutes for the indicator. Removing them at the same time avoids a permanent title-bar regression: the indicator only replaces them if they are gone; leaving them creates a duplicate-navigation clutter state. |
| 2026-06-28 | **Prod probe before apply is mandatory migration discipline.** Before applying any migration that adds a column (or any additive schema change), run a live column-existence query against the prod tab to confirm current state — not docs, not memory, not the log. Today's discipline: `\d table` or `information_schema.columns` query in the prod SQL editor, result attached to the session before any `ALTER` is issued. Prevents duplicate-column errors and documents prod state at the moment of decision. |
| Jun 29, 2026 | **`SPEC` mid-session command added.** Lightweight, repeatable feeder distinct from SESSION END: emits one implementation spec (grepped anchors + str_replace/SQL + test + LOG_SEED), no session-log/roadmap/next-session. Files into `docs/` as a durable record matching the existing `SPEC_*.md` family (NOT delete-after-merge). Used successfully across 4 SPECs this session. Separates the frequent "hand a change to Claude Code" act from the once-per-session close. |
| Jun 29, 2026 | **Supabase is the source of truth for display names; Clerk is auth-only.** The app reads names (and, going forward, roles/RBAC) from Supabase, never Clerk. Clerk holds a name only as a write-only mirror (kept accurate for its own transactional emails / signup-typo correction) that the app never reads for display. Keeps Clerk portable for a future Harbour-native RBAC layer across the fleet. |
| Jun 29, 2026 | **Session-bootstrap effects key on IDENTITY, never cosmetic attributes.** `fullName` in Effect 1's deps was a latent bug — a display-name change re-fired full session setup and wedged the loading state. Bootstrap re-runs on `userId`/`clerkId`/`email` only. |
| Jun 29, 2026 | **Harbour membership primitive: explicit-accept switches the active view; passive/admin grant does not.** Encoded by gating the invite auto-switch on `joinedId` (set only on explicit invite-accept) rather than user-history. A future passive/admin-provisioned join creates a membership without `joinedId`, so it correctly does not move the user's view. First fleet app establishes the pattern OurKeep et al. inherit. |
| Jun 29, 2026 | **Household-scoped UI state must reset on active-household change.** Surfaced as a systemic pattern (join banner, invite link, near-miss desync) — UI state describing a specific household must clear when the user switches or creates a new household. Elevated to the next-session audit goal. |
| Jun 29, 2026 | **SESSION START needs a sync + airlock gate.** A `git fetch` + behind-check and a "handoff/ not empty" check at session start. Tonight surfaced FOUR repo-state surprises (63-commit-stale machine, orphaned June-13 handoff, snapshot-vs-working-tree drift, "is the batch pushed?" at promote) — each caught by chance or by running the actual git command, not by assumption. The gate makes repo state visible at session open. |
| Jun 29, 2026 | **Vanity `.app` domains are sayable front doors; the Harbour is the house.** The Harbour (shared identity + combined fleet data + OurExperience) is the house; domains label the door, never address the house — so doors can repaint/multiply and houses can unify without changing what users type. |
| Jun 29, 2026 | **Auth domain stays singular and platform-owned from the start; vanity domains may multiply freely.** Protects Phase II shared-login across Our* apps and lets ourpoker opt out later without a migration. |
| Jun 29, 2026 | **`ourprovisions.app` is canonical; `velayo.ai` subdomain redirects to it** (redirect direction `velayo.ai` → `.app`). Canonical = the sayable, brand-reinforcing URL. Pending a check of current `velayo.ai` contents. |
| Jun 29, 2026 | **Auth-domain unification deferred (Phase II / app #2, KISS).** Near-term domain work must remain auth-neutral and reversible. |
| Jun 29, 2026 | **Catalog-row UI comes from ONE shared component.** Search is a filter over the dataset and must never substitute a different row presentation or behavior — steppers, swipe, and price line are identical filtered vs unfiltered. (Established with the `CatalogItemRow` extraction; stepper parity done, swipe parity pending.) |
| Jun 29, 2026 | **Manage-household surface to be re-sorted by frequency × gravity.** Open-door actions (invite, create) earn prominence on-thesis; heavy actions (delete, leave) get a quiet home, never front-door volume. (Direction set; design pending.) |
| 2026-07-03 | **Receipt import pipeline is source-agnostic: capture→normalize→reconcile→review→commit core written once; photo/email/API are thin adapters.** Avoids coupling the core logic to the photo path. |
| 2026-07-03 | **Review is confidence-gated: lines at/below 0.80 `match_confidence` surface to the human; above auto-commit but stay auditable.** Requires honest confidence scoring — gate is theater without it. |
| 2026-07-03 | **`price_hint` = rolling average of last-N (N=5) `receipt_items.price`, recomputed on commit — a denormalized cache; `receipt_items` is source of truth. No counter column.** |
| 2026-07-03 | **Alias learning table DEFERRED; `match_source` column is the hook and `receipt_items` (`match_source='human'`) IS the training set.** Backfillable in Phase 3 with one query. Never hard-delete confirmed receipt_items. |
| 2026-07-03 | **v1 receipt does NOT write `list_items` (mark-bought loop is Phase 2+); `session_id` column present but unused so the loop wires in later with no migration.** |
| 2026-07-03 | **Two-tier matching (Tier 1 deterministic free / Tier 2 AI paid) is a cost-shifting design: paid fraction shrinks as catalog fills — gets smarter AND cheaper month over month.** |
| 2026-07-03 | **Receipts are the purchase-history SUBSTRATE (data spine feeding UC-4→UC-11), not a standalone Phase 3 feature. Schema must serve questions not yet visible.** |
| 2026-07-03 | **Coupons/discounts/returns are first-class savings/spend data (`line_type`), not noise to discard** — serves UC-6 gamification and UC-15 returns. |
| 2026-07-03 | **[Velayo OS] Canonical Harbour fleet reconciled to 8 apps:** OurProvisions (live), OurManifest, OurKeep, OurChef, OurDiscovery, OurGarden, OurGames, OurHelper. Supersedes the 6-app list in `Velayo_Harbor_Investor_Narrative.docx`. OurKeep + OurGames have status only — no user-facing definition yet (OPEN). |
| 2026-07-03 | **[Velayo OS workflow] Parallel design chats do not each emit a handoff — one chat owns `design_handoff.md`, the other feeds it facts, producing one merged dated entry.** Prevents same-date SESSION_LOG collisions/overwrites. |
| 2026-07-03 | **[Velayo OS workflow] DB truth is a prod query, not a doc.** Applied migration state must be verified against `parpauldmbetptkmdwbd` by direct query before promoting. A claim in a file is not the same fact as applied state. (Reaffirmed from two directions this session.) |
| 2026-07-05 | **"Come aboard" is a questionnaire, not a plain CTA.** Completing it is the signup. The friction qualifies applicants ("willing to give to get") and pre-validates roadmap phases with GTM data. |
| 2026-07-05 | **Beta signups stored in Supabase as mission-control table #1** — operational telemetry owned by Velayo, distinct from product tables owned by households. `beta_signups` is the first brick of a mission-control layer. |
| 2026-07-05 | **Anon-key insert path, no Clerk.** A beta applicant has no Clerk identity yet; the insert goes through Supabase's public `anon` role. First place in the system touching the DB without Clerk. Correct and deliberate. |
| 2026-07-05 | **Landing page = "sine wave": Boardroom → Trough → Ah-ha → Vision → Front Door.** Questionnaire at the END; problem-led opening builds intent that peaks there. Lead with the wallet, land on the life. |
| 2026-07-05 | **Savings claim discipline: cite ~$10–13/trip (≈10%), never a bare "10–20%".** Upper range conflates bundled tactics (store-brands, sales) with the list alone. Anchor = Davydenko & Peetz (2020), J. Consumer Behaviour (randomized → causal). |
| 2026-07-05 | **Questionnaire field test = "does the answer change a decision in the next 6 months?"** Cut income (privacy weight, wrong tone), age (no near-term decision), "do you cook?" (identity, gameable, Phase 5). |
| 2026-07-05 | **Ask about behavior your next phase touches; let downstream reveal itself in free-text.** `list_method` (meals/staples/mixed) tests meal-planning-driven list priority as behavior, not the "do you cook?" identity. |
| 2026-07-06 | **Adopt a 45-day high-velocity "rolling thunder" beta (~6 weekly beats) instead of one polished launch.** Audience is friends/social connections invested in Dan's journey, not feature-evaluators; the cadence itself is the engagement product ("we heard you, we shipped again"). Bar moves from "flawless" to "alive and honest." Polish backlog becomes per-beat ammunition, not debt. |
| 2026-07-06 | **Beat order fixed for 0–2, held open for 3+.** Beat 0 = launch floor + two-way channel; Beat 1 = Shop/Filter (first visible ExD); Beat 2 = receipts (first AI). Beats 3–6 deliberately unplanned so "we heard you" is real, not slogan. |
| 2026-07-06 | **Two front doors, one list: velayo.ai (brand/journey) + ourprovisions.app (product) both feed one Mailchimp audience.** Build one capture, two front-ends — never two parallel signup systems. |
| 2026-07-06 | **Drop the landing-page beta questionnaire. Mailchimp email capture replaces it for beta.** The questionnaire was a marketing surface solving a problem (convince a stranger) Dan doesn't have yet. `beta_signups` table retained for future structured intake. |
| 2026-07-06 | **Signup automation stays capture+notify with a self-serve "come aboard" link, gated on the cold-start walk passing.** Full self-serve is honest only once a true zero-state user is verified to onboard cleanly. |
| 2026-07-06 | **Install is optional; the link is the real front door.** iOS "add to home screen" is guided by the app (coach-mark), never by Dan walking someone through the Share menu. Goal: remove Dan from the install loop on both platforms. |
| 2026-07-06 | **Platform question settled: web (React) for the entire beta.** Native (Expo/RN) stays post-beta Phase 5 — App Store review would break the weekly deploy cadence and add install friction to the 3-minute bar. |
| 2026-07-06 | **Beat 1 = Shop/Filter controls (existing spec); Beat 2 = receipt scanning (existing specs) — reframed as headline ExD and "first intelligence" moments respectively, not quiet phase transitions.** |
| 2026-07-07 | **Staple model = single join table for ALL staples (`household_staples`), not "join for global + column for custom".** Two sources of truth = drift risk (already flagged in ARCHITECTURE); one row-presence read path is the clean cut. `catalog_items.is_staple` left dormant (drop in a later cleanup migration, not 016). |
| 2026-07-07 | **`household_staples.catalog_item_id` = `ON DELETE CASCADE` — a deliberate, scoped exception to the "all FKs referencing `catalog_items` are `NO ACTION`" invariant.** Staples are disposable preference rows, not history; they should vanish with a deleted custom item, and CASCADE keeps migration 016 self-contained instead of unverifiable `CREATE OR REPLACE` surgery on the multi-step delete RPCs. Documented in ARCHITECTURE so a future pass doesn't "correct" it back. |
| 2026-07-07 | **Diagnosis-before-fix confirmed correct: the staples/filter cluster was TWO independent bugs, not one.** Bug A = `is_staple`-on-shared-catalog RLS block (`NULL in (...)` → 0 rows, silent); Bug B = cold-start `catalogMap`-empty-at-first-paint race (independent of the staple architecture). Stapling them into one fix would have been wrong. Reinforces "diagnose against a live query, decide one-bug-vs-two before writing." |
| 2026-07-07 | **Cold-start gate = PARTIAL GREEN → signup email stays on the PERSONAL variant.** Bugs A + B fixed and prod-verified, but the false-removal-banner leak fired on prod during staple churn → do NOT flip to self-serve until the banner leak is fixed. A genuinely-broken cold-start is P0; a rough-but-working one can flip. |
| 2026-07-07 | **Migration number 016 = `household_staples` (live dev+prod).** The queued `receipts` (previously earmarked "016") and `beta_signups` ("017") in ARCHITECTURE must renumber. Consistent with the "filing numbers are folder convention, not load-bearing; truth is a query against the environment" decision — the applied migration owns the number. |
| 2026-07-09 | **False-removal-banner P0 root cause = deliberate-loss / watchdog-poll race, not a transient-empty read.** The 30s `checkPresence` poll and the deliberate delete/leave handlers both react to one membership loss; the poll fires the loud notice during the reconciliation gap. Instrumentation (a temporary DIAG capture) disproved the transient-empty theory and the staple-churn correlation — churn only mattered because long sessions cross poll boundaries. |
| 2026-07-09 | **Guard deliberate-loss at the handler layer, BEFORE the destructive RPC — not inside the resolver.** v1 set `resolvingRef` on resolver entry, but the RPC runs first (~2s on 3G), leaving an unguarded window a poll can land in. v2 raises `deliberateLossRef` at the start of the handler and clears in `finally`, spanning RPC + resolution. Mechanism B (self-clearing ref) at timing A (before the action). The durable rule: a background watchdog must be guarded across the *whole* deliberate action, from before its first async boundary. |
| 2026-07-09 | **Blank replay is "wait-and-reload" before it's "broken."** When capture is confirmed and replay beacons return 200, a blank Splunk replay player is render latency, not misconfiguration. Do not chase config/version/support theories until a reload + wait is ruled out. (Cost ~1hr this session before the boring cause surfaced.) |
| 2026-07-09 | **Confirm-before-fix, via instrumentation, beats fixing from an uncertain memory.** The reporter's recollection ("no membership action") was wrong; a temporary diagnostic log at the exact decision point produced the true firing state. Same discipline that landed the blank-catalog fix first-try. |
| 2026-07-09 | **Beta CTA is RANKED, never forked** (primary "Come Aboard" → beta; secondary "Follow the journey" → Mailchimp). A cold blog reader gets a high-intent ask with a graceful soft landing, without splitting attention. |
| 2026-07-09 | **Beta is OPEN (fill form = you're in), so the seven-question questionnaire is onboarding intelligence, not an application.** Same `beta_signups` table/RLS, different copy posture — collect setup signal, not admission judgment. |
| 2026-07-09 | **Post-signup path = fewest clicks into the app; welcome email is a parallel keepsake, not a gate.** Questionnaire email carries into Clerk sign-up (verified Clerk feature — `initialValues`/query string; pre-fill still hits Clerk's own OTP, not one-click entry). |
| 2026-07-09 | **Beta success = TWO numbers: 10 users touch key features (breadth) AND ≥50% invite a second person (depth / CI test).** Depth is the real thesis experiment; breadth without depth is a false pass. Growth cadence in 10s: 10→100→1k→10k. |
| 2026-07-09 | **The second-person INVITE is the activation pivot.** Collaboration is the aha but solo activation is allowed, so the invite is the gate to the thesis — the welcome email + first nurture beat must drive the invite above all else. |
| 2026-07-09 | **Receipt scanning REMOVED from the beta-10 feature/nurture set** — it's Phase 3, no beta user reaches it. Nurture series is nurture-now (live) vs nurture-later (Beat 1/2 features become "we heard you" emails on ship). |
| 2026-07-10 | **Duplicate-create defense = client button-disable, NOT a DB unique constraint.** `households.name` is intentionally non-unique (two real households may share a name), so a server unique constraint is wrong. The reproduced trigger was a 259ms human double-tap → disabling the button in-flight kills it. Server idempotency (fuzzy `(created_by, name)`-within-a-window guard) stays a WATCH item, promoted only if a non-human `created_at` signature appears (retry hook defeating the button). Fix the observed trigger; don't add fuzzy-dedup risk for an unobserved one. |
| 2026-07-10 | **Invite = Web Share, feature-detected, not UA-sniffed.** Branch on `navigator.share` existence; one tap lands the invite in a real conversation with a pre-written message (Copy is the fallback + sole action where Share is absent). The Beta 1 depth metric counts *sends*, not copies — the flow must actually send. |
| 2026-07-10 | **Invite link is lazy-generated on intent (Option B), not on panel open.** Generate on the first of {~500ms idle-after-open, explicit Send/Copy tap} so a fast open/close writes no `household_invites` row — keeps the table honest with no inert self-expiring codes. The idle timer means the link is normally ready by Send-tap time; the tap path guarantees a generate if the user beats the timer. `prepareInvite` is idempotent (guards on url/in-flight) so timer + tap can't double-write. |
| 2026-07-10 | **Invite message is a single source of truth (`INVITE_MESSAGE`), used by both the preview and `navigator.share`** — so what the sender previews is exactly what the recipient receives, and it echoes the welcome-email "bring your first mate" language (one CTA vocabulary email → in-app → recipient). |
| 2026-07-10 | **Web Share email-subject blanking accepted as a platform limitation.** `navigator.share`'s `title` is passed correctly, but some clients (notably iOS Mail) drop it. The full invite + link ride in `text` across every target, so nothing is lost. A `mailto:` fallback (guaranteed subject) is a future "we heard you" beat only if email becomes a primary invite channel — not pre-launch work. |
| 2026-07-10 | **Browse un-added rows show "Add", not a zero-quantity stepper.** Invariant: on-list ⇔ qty > 0 ⇔ stepper; not-on-list ⇔ qty 0 ⇔ "Add". Zero is never rendered — kills the ambiguous zero-state and quiets most rows to one word. Honors the boundary `updateQty` already enforces (`Math.max(0, qty)`, `qty <= 0` = removal). |
| 2026-07-10 | **Stepper and Add share one ghost-pill treatment** (transparent-on-cream, clay-lo `#C9A97A` outline, no white fill, no shadow). The "pill sticks out" problem was really visual-weight mismatch: bright white stepper vs. transparent Add. Matching them makes the two states read as one control family. |
| 2026-07-10 | **iOS = WebKit for all browsers; iPhone is the source of truth for touch-state bugs.** Sticky-hover is a WebKit quirk. Fix pattern: guard `:hover` fills behind `@media (hover: hover)` + blur-on-tap for anything that re-renders under the touch point. |
| 2026-07-10 | **Spec lifecycle: authored → `active/` → `built/` (on ship) → `retired/` (if superseded).** Built specs are archived as forensic record, not deleted — the truth table / rejected alternatives / "why" are the best record when a shipped feature later breaks. Retired ≠ built: retired = never-shipped-or-dead-diagnostic; built = live in product. Kept separate so `built/` is a trustworthy map of shipped behavior. *(Velayo OS convention — reorg DESIGNED, not yet executed; see NEXT.)* |
| 2026-07-10 | **Production domain confirmed = `ourprovisions.velayo.ai`.** An earlier `ourprovisions.app` reference this session was a misstatement; CLAUDE.md was already correct — no doc change. (`.app` remains the parked future canonical-front-door plan, not live prod — see the `Jun 29` `.app`-parked decision.) |
| 2026-07-10 | **At declutter merge: RETIRE `SPEC_filter_show_hide.md`** (superseded by the declutter cycle) — do not leave it in `active/` once the reorg runs. |
| 2026-07-11 | **When approved-mockup and spec-prose disagree, the mockup wins.** The declutter icon and the stateless grouped/flat behavior both came from `cycle_dual_readout.html`, not the spec's prose (which had an internal contradiction and a stale "∨" description). Design analogue of "truth is a query against the correct environment, never the doc." |
| 2026-07-11 | **Declutter cycle is view-only; phase is per-device UI state, list data is shared+realtime.** A mutation from another member changes data; the local phase holds and re-renders the new data under it. Verified: an incoming check/uncheck re-derives the hide filter off live checked-state without resetting phase. Generalizes to any future per-device view control over shared data. |
| 2026-07-11 | **Reset-to-grouped is the correct default (not persisted-flat).** Grouping is the everyday shop-by-aisle view; flat A–Z is the hunt-for-one-item escape hatch. Persisting flat would silently strip grouping until the user noticed. Removed `op_showCategories`; Shop's view mode is now derived from cycle phase. |
| 2026-07-11 | **Beta prioritization axis = threat to beta success (breadth + depth), not bug-vs-UX type.** Shared-list integrity bugs + invite-arrival onboarding are Tier 1 because they protect trust and the depth (second-invite) metric. One goal per session; bug-fix (build) and nav/affordances (design) pull in opposite directions, so they're sequenced, not merged. |
| 2026-07-11 | **★ Invisible-affordances pattern (candidate principle, not yet ratified).** Four field reports share one root: critical actions reachable only through hidden gestures / camouflaged chrome that work once a human reveals them (tabs not seen as tabs; nav unreachable from a long scroll; browse→list invisible; swipe-to-delete undiscoverable). Two-part fix to ratify in a design session: (a) visible affordance for critical actions, not only a gesture; (b) one reusable milestone-keyed coachmark primitive (not per-feature one-offs). |
| 2026-07-11 | **Household photo header → Beat 1 (next week).** Per-household imported photo behind a legibility scrim; personalizes ("this is the place") + instant household identity — a direct ExD-as-art expression. Scrim = **top-and-bottom gradient** (dark where type sits, photo clear through the middle) over even-wash (too flat) / bottom-anchored (wordmark softens over busy image). Sacandaga framing = **Slice B (middle)**: cottage, flag, water line. Must include a **framing/reposition step at import** — vertical photos need a user-chosen horizontal slice; the app can't guess lake-vs-faces. |
| 2026-07-11 | **Invite flow validated in the field.** Aidan invited a second person first-try, unprompted in mechanics — the CI/activation (depth) thesis firing live on a real first-time user. |

---

## DONE — Shipped & Live

### Anon catalog exposure — CLOSED ✓ *(2026-07-18 — live prod data exposure, migration 022 dev+prod, browser-verified)*

| Feature | Date | Notes |
|---|---|---|
| **Anon catalog PII exposure — closed** | Jul 18, 2026 | The `{anon}` SELECT policy on `catalog_items` (named `"Anyone can read global catalog items"`) had predicate `(deleted_at IS NULL)` with **no `is_global` check** — all 183 rows incl. 133 household-custom free-text rows (a beta user's parent's medication schedule + dated caregiving notes) were readable by any anon visitor with the publishable key. **Migration 022** (`migrations/022_anon_catalog_global_only.sql`) `ALTER POLICY … using (is_global = true and deleted_at is null)` — `ALTER`, not DROP/CREATE, so no window without an anon policy. **Applied dev + prod, verified via `set local role anon`** (prod 50/0 custom, dev 38/0) **and incognito browser Network (183 → 50).** The `{authenticated}` policy (`is_global OR is_member_of`) was already correct, untouched. Spec: `docs/specs/built/SPEC_anon_catalog_exposure.md`. Follow-ups → NEXT (anon-surface audit; disclosure decision) + defense-in-depth client `is_global` filter (not built; 022 is the real fix). |

### Landing-page build + migration 021 ✓ *(2026-07-17 — new standalone repo + first anon-write table; page not yet live)*

| Feature | Date | Notes |
|---|---|---|
| **Migration 021 — `beta_signups` applied dev+prod** | Jul 17, 2026 | First unauthenticated write in the system. `anon` may INSERT only; the **absence of a SELECT policy is the security** (keeps `status`/`fit_note` private). Verified on both envs: 14 columns, `relrowsecurity=true`, one policy (`public can apply` / `a` / `{anon}`). Record: `migrations/021_beta_signups.sql`. Resolves the 021 double-claim — referral primitive → 022. The critical anon-cannot-read test needs the deployed page (see NOW). |
| **`ourprovisions-landing` built + pushed** | Jul 17, 2026 | Standalone static repo (`Velayo-ai/ourprovisions-landing`) — no React/Supabase-client/Clerk; chrome/content split; 4 images de-base64'd; hero `100% auto`/`center 42%` preserved; mockbar stripped, nav `top:0`. **Arrival mechanic browser-verified:** nothing parsed (first/last separate), `name` joined, `multi_household` boolean-coerced, insert fires with `apikey`+`Bearer`+`Prefer`, and the door opens **even when the insert fails**. Captions rewritten (Shop → "Get in, get out, get it right"; Browse → "The stuff you buy every week") and footer restructured (3-col grid, `Velayo · Privacy · Terms`, absolute `velayo.ai` URLs) — synced from the mockup, verified desktop+mobile. **NOT live yet** — deploy + DNS in NOW. |

### Prod backup floor + contributor A-fix closed ✓ *(2026-07-16 — no app code; Supabase + dev→main merge)*

| Feature | Date | Notes |
|---|---|---|
| **Prod backup floor — first backup since March** | Jul 16, 2026 | `pg_dump 17.10` → custom-format dump `ourprovisions_prod_20260715.dump` (396 KB, `--no-owner --no-privileges`). TOC-verified (`pg_restore --list` shows TABLE DATA for all three core tables, all RLS policies, FK constraints incl. F0's `list_items_household_catalog_unique`, realtime publications, `get_list_items_for_household`), then copied off-instance to Drive. `*.dump` added to `.gitignore`. **A floor, not a strategy** — the dump ages from this moment; backup cadence decision → NEXT. `--no-owner --no-privileges` is correct for Supabase (platform role grants don't transplant; RLS/constraints/RPCs/publications survive). |
| **Contributor A-fix — verified + merged** | Jul 16, 2026 | The Shop name-line display fix (`3182afc`) dev-verified two-account and merged dev→main (`d972c88` → `abc5f15`), prod-verified. Name renders from the contributor ledger; own items show no name line; a remove→revive attributes to the real contributor. See the A-fix row in the F0+F1b block below for detail. |

### Prod outage restored + user-visible alerting stood up ✓ *(2026-07-15 — no code; Splunk O11y + Supabase dashboards)*

| Feature | Date | Notes |
|---|---|---|
| **Prod restored — wedged PostgREST** | Jul 15, 2026 | Prod hung on "Loading your provisions…". Root cause: a wedged PostgREST at the **Supabase platform layer** — not app code, data, or load. Diagnosed by killing hypotheses: `pg_stat_activity` = 6 idle infra connections + zero app queries (DB unreached); error = Cloudflare→origin **522**, uniform across every Data API endpoint; IOPS = 3 of 3,000; dev healthy on identical code. Fix: **restart the project.** New rule: *"522 across every endpoint while Postgres sits idle = restart, not debug."* |
| **Splunk RUM detector (JS errors)** | Jul 15, 2026 | `rum.client_error.count` · Sum · `sf_environment:production` · above 1 · trigger immediately · Major · runbook → prod Supabase dashboard. Backtested by Splunk's estimator to exactly 1 alert in the prior week (the real outage); a first config (threshold 3 / 80%-of-5m) estimated 0 and would have missed it. **Threshold (1) untuned against real noise — see NEXT.** |
| **Splunk Synthetics API test (Data API uptime)** | Jul 15, 2026 | `OurProvisions PROD — Data API`: `GET https://parpauldmbetptkmdwbd.supabase.co/rest/v1/catalog_items?select=id&limit=1` w/ `apikey` (anon key) · AWS N. Virginia · 5 min · assert 200 · uptime detector <90%, 2 consecutive violations, Critical. Exercises **PostgREST**, not the Vercel HTML shell (which stayed green through the outage). Verified live at HTTP/2 200 in 122ms before activation. Anon key is safe in the header (already ships in the client bundle); `service_role` never used for monitoring. |
| **Detector on the `Velayo Inc.` browser test** | Jul 15, 2026 | The pre-existing Splunk browser test had been running with **zero alerting** — added a detector. **Coverage boundary: prod only** (dev is deliberately unmonitored — no self-paging for dev). |

### Shared-list integrity hardening — F0 + F1b ✓ *(2026-07-15 — dev→main `28539af`, F1b prod-verified)*

| Feature | Date | Notes |
|---|---|---|
| **F0 — `uq_live_list_item` partial-unique on `list_items`** | Jul 15, 2026 | Migration **020**: `create unique index uq_live_list_item on list_items (household_id, catalog_item_id) where deleted_at is null` — the list-layer structural twin of 019. At most one live list row per catalog item per household is now guaranteed by the DB (F2's `.eq("id")` prevented the symptom; this removes the class). Pre-req live-row dup census returned 0 on prod; applied by hand to dev + prod, clean CREATE both (itself the proof). `.sql` record committed (`0554587`). Reversible: `drop index uq_live_list_item;`. Per `docs/specs/built/SPEC_F0_uq_live_list_item.md`. |
| **F1b — hide→re-add no longer stomps shared quantity** | Jul 15, 2026 | Hide is a lens, not an edit. `searchResults` excludes hidden items, so a hidden-but-live item showed "No results" and the "Add" verb routed through `updateQty(name,1)` — stomping the shared qty 10→1 across the person boundary. Fix (client-only, `85f4a69`): new `unhideItem` un-hide-only primitive (never touches `list_items`); `updateQty` create branch resolves a hidden exact-normalized name to the existing catalog row (hidden ≠ new); search surfaces a hidden-but-live exact match as a **reveal card** (un-hide only, no qty write) instead of a false "add as new." Verified two-account on dev AND prod. Per `docs/specs/built/SPEC_hide_readd_quantity_stomp.md`. |
| **Add-pill affordance drift cleared** | Jul 15, 2026 | Search no-results "Add '<query>' to your list" row used the old solid `+` circle, inconsistent with every Browse catalog row (`.add-btn` pill). Swapped to the pill (`c180b73`, App.js, visual-only — row still opens the category picker → `addSearchedItem`). |
| **Contributor-attribution display fix (A-fix) — VERIFIED + MERGED ✓** | Jul 16, 2026 | DH saw "Dan Test User" on items whose only contributor was Dan Holmes: `list_items.added_by` is immutable (INSERT-only) and drifts from `list_item_contributors`; the `≤1 contributor` UI branch fell back to it. Shop name-line now derives from the ledger — `contributors[0].fullName` when exactly one contributor, nothing when zero; `isOwnItem` keys off the sole contributor's `clerkId` (`3182afc`, App.js). **Dev-verified two-account against all three assertions, merged dev→main (`d972c88` → `abc5f15`), prod-verified 2026-07-16.** Test-script correction: there is no "remove" button — the remove path fires from `updateQty` at qty 0, and only the *other* user's window can read the result (`isOwnItem` suppresses your own name line). Remove-confirm dialog still reads `added_by` (repoint later, with the build-gated ledger work). |

### Shared-list data-integrity — Bugs 1, 2, 3 ✓ *(2026-07-13 — dev→main `851b5ae`, prod-verified)*

| Feature | Date | Notes |
|---|---|---|
| **Bug 1 — duplicate catalog item on re-add (catalog fork)** | Jul 13, 2026 | Prod query overturned the list-layer hypothesis: 3 list rows pointed at 3 distinct `catalog_item_id`s → the **catalog** was forking. Fixed at write-time by migration **018** (`insert_custom_catalog_item` rewritten sql→plpgsql, idempotent: reuse a live same-name row matched on normalized name — lower+trim+collapse-ws — prefer global then oldest custom, store original casing) + DB backstop migration **019** (partial unique indexes `uq_global_catalog_norm` + `uq_custom_catalog_norm`). Census found + cleaned **12 fork sets** on prod (10 global double-seed dups Mar-20/Mar-30 prod-only, 2 custom) via reversible soft-delete/merge; 0 remaining. Both migrations applied dev+prod, `.sql` records committed (`dcdf2bc`, `7173f6a`). |
| **Bug 2 — check one, both check** | Jul 13, 2026 | `toggleChecked` wrote `.eq("catalog_item_id", …)`, flipping every live row sharing that catalog id. Now writes `.eq("id", listItemId)` (unique row); `listItemId` threaded `newListRows[].id` → `shoppingList` item → all four `toggleChecked` callsites. Commit `b85dcbf`. |
| **Bug 3 — toggle bounce / ~3 taps to stick** | Jul 13, 2026 | The 2s poll called `setChecked(newChecked)` unconditionally, clobbering the optimistic value before the write committed. Added `pendingCheckRef` (mirror of `pendingQtyRef`): `toggleChecked` marks the item in-flight before the optimistic set and clears on settle; `loadListItems` preserves `prev[name]` for in-flight names. Commit `b85dcbf`; prod-verified incl. Slow-3G stress. |

*Remaining hardening F0 + F1b — SHIPPED 2026-07-15 (see the F0 + F1b DONE block above). `SPEC_shared_list_integrity.md` graduated `active/ → built/`.*

### docs/ reorg — spec lifecycle folders ✓ *(2026-07-11 — commit `0303397`, Velayo OS hygiene)*

| Feature | Date | Notes |
|---|---|---|
| **Spec lifecycle folders created + flat docs/ sorted** | Jul 11, 2026 | Created `docs/specs/{active,built,retired}/` and moved ~43 files by scope→lifecycle in one OS-scoped commit (`0303397`): 31 → `built/` (shipped/forensic), 10 → `active/` (open/in-flight), 1 → `retired/` (spent `DECLUTTER_BUILD_HANDOFF`). `docs/` root now holds only the 3 canonicals + `DEV_SETUP` + `EVIDENCE` + `mockups/` + `specs/`. 3 unlisted specs bucketed via `git log` (`fix_authuid_rls`, `full_name_sync` → built; `beta_signups` → active). Misfiled `007_dev_restore_role_grants.sql` → `migrations/` (**dup 007 number — renumber TBD**). Per `docs/specs/built/SPEC_docs_reorg.md`. **Deferred:** airlock wiring, velayo-os leg, spec-path reference sweep (all now in NEXT). |

### Shared declutter cycle (Browse + Shop) ✓ *(2026-07-11 — dev→main `3d256aa`, on prod; prod-verify pending)*

| Feature | Date | Notes |
|---|---|---|
| **3-phase declutter cycle, both tabs** | Jul 11, 2026 | One 46×46 `CycleIcon` shared by Browse + Shop, cycling 0→1→2→0 (grouped → noise-hidden grouped → flat A–Z). Encodes two axes in one control (bg light/dark = shown/hidden via `.on` class; lines tapering/equal = grouped/flat). Built in 3 dev-verified commits: Shop hide-checked + flat (`80f1a03`); Browse hide-pills + flat (`419a5b9`); unify → shared `CycleIcon` + `FlatHeader` (`7b18db4`). Descriptor line states the consequence when decluttered; cycle changes **view only** (never clears filters/checked-state); phase resets to 0 on tab/household switch. Removed the old `op_showCategories` persistent grouped/flat pref (view mode now derived from phase). Two-account realtime verified (Shop). Per `docs/SPEC_declutter_cycle.md` + `docs/DECLUTTER_BUILD_HANDOFF.md`. **Superseded + removed from NEXT:** the standalone "Browse grouped/flat toggle." |

### Beat 1 — Browse stepper ExD polish ✓ *(2026-07-10 — dev→main, on prod)*

| Feature | Date | Notes |
|---|---|---|
| **Quantity stepper unified as a pill** | Jul 10, 2026 | Replaced the three floating +/−/count circles with one rounded pill (`ae7708c`, App.js CSS-only). Mockup-before-code; 3 variants eye-tested against the real palette. First step of the ExD polish arc; refined further by the match-to-Add commit below. |
| **Add → Stepper on Browse rows** | Jul 10, 2026 | Un-added rows render a single "Add" ghost pill; tapping adds (qty 1) and reveals the −/N/+ stepper; − at qty 1 removes the item and snaps the row back to "Add" (existing `updateQty` removal path — automatic, no extra logic). Zero is never rendered, killing the ambiguous zero-state. Conditional render in `CatalogItemRow` (`a13530a`, App.js); both call sites unchanged. Per `docs/SPEC_add_to_stepper.md`. |
| **iOS/WebKit sticky-hover fix on Add** | Jul 10, 2026 | The Add button could latch its clay `:hover` fill after a touch sequence (tap Add → +/− to 0 → touch another row) because mobile Safari holds `:hover` on a freshly re-rendered element. Fix (`dea88a1`, App.js): moved the `:hover` fill inside `@media (hover: hover)` (kept `:active` unconditional for tap feedback) + `e.currentTarget.blur()` in the Add `onClick`. Promoted with the stepper work (`27d881b`), prod-verified. |
| **Stepper matched to Add ghost treatment** | Jul 10, 2026 | Brought the `− N +` stepper to the "Add" pill's ghost look so both read as one family (`1cd0c6e`, App.js CSS-only, atomic): `.qty-controls` fill `#fff → transparent`, box-shadow removed, outline + `.qty-display` dividers `#e8ddd0 → #C9A97A` (matching `.add-btn`). Promoted dev→main (`85de8e0`); dev-preview confirmed before promote. |

### Beat 1 — Invite pivot + churn hardening ✓ *(2026-07-10 — dev→main, on prod)*

| Feature | Date | Notes |
|---|---|---|
| **Invite Web Share flow (Beta 1 activation pivot)** | Jul 10, 2026 | Reshaped the invite affordance to close the copy→send gap — the Beta 1 depth metric counts *sends*, not copies. `navigator.share` (feature-detected; Copy is the fallback + sole action where Share is absent) with a single-source brand-voice `INVITE_MESSAGE` so the preview === what's sent. Lazy-generate (Option B): `prepareInvite()` fires on a ~500ms idle-after-open timer OR an explicit Send/Copy tap — a fast open/close writes no `household_invites` row; idempotent, in-flight-guarded. Three render states (preview / preparing / in-voice error retry). Trigger renamed "Invite someone aboard" + teal to match the welcome-email "bring your first mate" vocabulary. Client-only (`8c62315`, App.js); `createInvite` unchanged. Per `docs/SPEC_invite_share_flow.md` + `docs/mockups/mockup_invite_flow.html`. Web Share needs a real mobile browser + HTTPS to verify. |
| **Duplicate-household creation — FIXED (client)** | Jul 10, 2026 | Root-caused via prod queries to a creation-race: the Create button's async onClick awaited `createHousehold` for ~2s on 3G but was never disabled, so a 259ms human double-tap fired two `create_household` RPCs → two distinct ids, each with its own owner membership (`04:07:11.952` + `04:07:12.211`). Fix (`565b133`, App.js): dedicated `creatingInFlight` state raised before the await, cleared in `finally` — button disabled + dimmed + "Creating…", plus an early-return guard closing the commit-to-rerender window. Diagnosis-first per `docs/SPEC_duplicate_household_repro.md`; fix per `_fix.md`. Server idempotency left as a WATCH item; **Part 2 prod cleanup of existing dup rows still pending (manual).** Client-only. |
| **Removed dead `selfDepartureRef`/`markSelfDeparture`** | Jul 10, 2026 | Scaffolding superseded by the deliberate-loss guard (`deliberateLossRef`); exported in the context value but consumed nowhere in `src`. Removed the ref, the callback, and its context-value entry (`5da1c37`); synced ARCHITECTURE's three stale mentions. Own commit (touches the exported context API). |

### Beat 0 — Bug fixes ✓ *(2026-07-09 dev; P0 PROD-VERIFIED 2026-07-10)*

| Feature | Date | Notes |
|---|---|---|
| **False "No longer a member" banner P0 — FIXED + PROD-VERIFIED** | Jul 9–10, 2026 | Root-caused a non-reproducible bug to a race: a deliberate delete/leave and the 30s `checkPresence` watchdog both react to one membership loss; the poll fired the removal notice during the reconciliation gap. Fixed across a four-commit arc — DIAG capture (`c9e330f`, temporary) → v1 `resolvingRef` + name fix (`a43a1ba`, reverts the DIAG) → v2 `deliberateLossRef` raised at the START of the delete/leave handler *before* the destructive RPC, cleared in `finally` (`081c641`, the fix that held). Guard spans the whole deliberate action so no poll lands in the gone-server-side-but-unreconciled window; external removals untouched (regression verified: DH removed DT from Test 101, notice still fired + named right). Also fixed banner name-correctness (stale sticky name → threaded `lostName` from pre-update `myHouseholdsRef`). Verified on dev (Slow-3G create/delete churn), then **PROD-VERIFIED 2026-07-10 — no observed defects; the signup-email-flip gate is now cleared.** `selfDepartureRef`/`markSelfDeparture` scaffolding subsequently removed (`5da1c37`). |
| **Blank-catalog on category-delete** | Jul 9, 2026 | Deleting a filtered category left its `rawName` in `selectedCategories`, collapsing the Layer-2 filter predicate to empty → blank Browse (reload/filter-toggle restored it, confirming client filter-state not data loss). Fix (`4d17c58`, App.js): evict `rawName` from `selectedCategories` on delete (mirrors the `householdCategories` eviction) + self-healing predicate that narrows only by categories that still exist, so no path (incl. a realtime delete from another device) can strand the view. Verified on dev. Client-only. |
| **RUM session-replay masking** | Jul 9, 2026 | Splunk replays came back fully masked despite intent to unmask the grocery UI. `sensitivityRules` used key `type:` where the Splunk recorder reads `rule:` → every rule silently skipped → recorder fell back to its privacy-first mask-everything default. One-word fix ×5 (`f9ae19e`, rum.js); selectors unchanged (unmask `body`, mask `input`/`textarea`, exclude Clerk). Follow-up flagged: `unmask body` exposes all rendered text incl. emails/identifiers in member lists/profile headers — add mask rules once class names are confirmed. |
| **Session replay rendering + unmasked** | Jul 9, 2026 | Chased a blank-replay red herring to ground — capture, ingest (200s), versions, console all healthy; the blank frames were **render latency, not a bug.** Replay is now a working diagnostic instrument (it surfaced the blank-catalog bug this session). |

### Beat 0 — Launch floor ✓ *(2026-07-07)*

| Feature | Date | Notes |
|---|---|---|
| **Join-activation — REOPEN prod-verified** | Jul 7, 2026 | The dev-green `pendingJoinId` fix (`1c5a916`) failed on prod: an existing user active in "My Household" who pasted an invite never moved — `just_joined_household_id` stayed set, lens never wrote (membership-propagation latency; the interrupted-window caveat became a confirmed prod failure). Root cause: flag consumption + `pendingJoinId` derivation were wired *inside* the banner effect, gated on `household.name !== "My Household"` — exactly false for that user. Fix (`c1ceab2`, App.js): banner effect is display-only; a durable/retriable effect derives the switch from the surviving sessionStorage flag on every relevant render, re-fires `switchHousehold` on each `myHouseholds`/`activeHouseholdId` change, clears the flag ONLY after `activeHouseholdId === joinedId`, and bounded-retries `refreshHouseholds` for late membership. Lens stays single writer. Verified on prod (existing user in prior household → auto-switch → banner → survives hard-reload). See `docs/SPEC_join_activates_household_ADDENDUM_reopen.md`. Client-only. |
| **Sign-in/sign-up cold-load fix** | Jul 7, 2026 | On a cold load the Clerk `SignInButton`/`SignUpButton` modal handlers aren't wired until `isLoaded`, but the buttons rendered enabled → a click before load was a dead no-op requiring a refresh (surfaced by a fresh deploy's cold JS cache + a laptop waking from sleep). Fix (`2249d0b`, App.js): gate on the already-imported `isLoaded` — while `!isLoaded` render disabled placeholder buttons (identical dimensions, only cursor+opacity differ → no layout shift), swap to live once loaded. Verified disable-then-trigger on Slow 4G. Client-only. |
| **Per-household staples — `household_staples` (Bug A)** | Jul 7, 2026 | `is_staple` was a single boolean on the shared `is_global=true` row (`household_id=NULL`) → tapping Staple on a global item silently no-op'd (the `catalog_items` UPDATE policy admits only `household_id in (memberships)`; `NULL in (...)` is never true → 0 rows, no error → reverted on the 20s poll), and the Staples filter read that shared flag. Fix (`949a8e9` + migration 016): new `household_staples(household_id, catalog_item_id)` join table, row-presence = stapled, single source of truth for global AND custom; RLS select/insert/delete on `is_member_of`; `catalog_item_id` `ON DELETE CASCADE`; backfilled existing custom staples (dev 1=1, prod 5=5); `get_list_items_for_household` repointed to derive `is_staple` via EXISTS; `toggleStaple`/catalog reads rewritten. Applied to dev + prod. Global staples now persist per-household with no cross-household leak. `catalog_items.is_staple` dormant. |
| **Cold-start empty-Browse race (Bug B)** | Jul 7, 2026 | A first-time user could land on an empty Browse until they tapped a filter (filters only narrow — the data was simply missing). Root: on a cold start the global-catalog read could return empty/errored before session/RLS warmed, then proceed — stranding `catalogMap={}` with `loading` already false and (on the error path) no poll armed. Fix (`7d1e3c2`, useProvisions): the ~50-row global seed set always exists, so an empty result is never legitimate → bounded retry (4 attempts, short backoff) that breaks as soon as rows arrive; zero delay on the normal path. Independent of the staple architecture. Client-only. |
| **New-user cold-start walk — PARTIAL GREEN** | Jul 7, 2026 | Ran the timed zero-state walk on prod with real data. Staples (Bug A) and empty-Browse (Bug B) both fixed and prod-verified. Gate result: PARTIAL GREEN — the false "No longer a member" banner leaked once during staple churn → signup email held on the personal variant (see NOW P0 blocker). |

### Velayo OS ✓

| Feature | Date | Notes |
|---|---|---|
| **The Harbour — company operating dashboard** | Jun 11, 2026 | Four-lane balance instrument (Business Foundation / Product / Marketing / Sales & Support). Leverage gauge (1 human : agent tasks, goal 1:20). Built + live at `harbour.velayo.ai`. |
| **Private hosting + push-to-deploy pipeline** | Jun 11, 2026 | `velayo-os` repo (private), Cloudflare Pages Git-connected, custom domain `harbour.velayo.ai`. |
| **Cloudflare Access gating** | Jun 11, 2026 | Zero Trust org, OTP "Crew only" policy (named emails + `@velayo.ai` domain rule). Verified end-to-end in incognito. |
| **SESSION END v2 pipeline + scope tagging** | Jun 11, 2026 | Chat emits `design_handoff.md`; Claude Code owns canonical write. v1 Drive-writing Scribe retired. [SCOPE] tags in CLAUDE.md. |
| **Retire v1 Scribe; rebuild project template as dual-mode** | Jun 16, 2026 | OurProvisions project instructions + `VELAYO_PROJECT_TEMPLATE.md` both on v2 handoff flow. Template is dual-mode (DESIGN vs HANDOFF). Scope tagging canonical in CLAUDE.md. |
| **Multi-household data spine** | Jun 17, 2026 | `ActiveHouseholdContext` built + wired in `App.js`. `get_my_households()` SECURITY DEFINER RPC (migration 001). Proven end-to-end on dev with two-household test user through real Clerk JWT. Bootstrap stopgap (migration 002) unblocks the crash. |
| **Authorization spine — `is_member_of` (003) + `list_items` write policies (004)** | Jun 17, 2026 | SECURITY DEFINER membership primitive (boolean, fails closed, `search_path` pinned); `list_items` write/update/delete RLS policies rewritten to `is_member_of`; `with check` added to UPDATE. Proven on dev (smoke test: true/true/false/false). Committed to repo. DEV ONLY — prod apply is immediate next step. |
| **Multi-household switcher** | Jun 17, 2026 | `useProvisions` re-scoped to `ActiveHouseholdContext` (two-effect split, `bootstrapped` state gate, GoTrueClient guard). Unified manage-household sheet: switch/create/rename/invite. `create_household` RPC + `households` RLS converted to `is_member_of` (migrations 005–006). Intermittent hang fixed; stale switcher fixed via `refreshHouseholds()`. DEV only; prod migration bundle pending. |
| **Authorization sweep — migrations 003–007 applied to prod** | Jun 18, 2026 | Contributor 403 root-caused to `household_members_select` (blinded inline joins). Migration 007 converted remaining five `get_current_household_id()` gates (`household_members` select, `waste_events`, `catalog_items` select+insert, `household_invites` insert) to `is_member_of`. Bundle 003–007 applied to PROD atomically — prod RLS now fully on `is_member_of`; `get_current_household_id()` gates retired from all write/read paths. |
| **Multi-machine dev environment + DEV_SETUP.md** | Jun 13, 2026 | Reproducible-from-git dev recipe: `.nvmrc` (Node 24), `.npmrc` (`legacy-peer-deps=true`), `docs/DEV_SETUP.md`. Committed `1409a5c`. |
| **Lake Surface stood up** | Jun 13, 2026 | DEV_SETUP recipe proven end-to-end: git clone → drop `.env.local` → npm install → npm start → Clerk auth. Node 24.14.0, peer-dep workaround validated. |

### Phase 1 Foundation ✓

| Feature | Date | Notes |
|---|---|---|
| React app scaffolded + deployed to Vercel | Mar 2026 | Create React App |
| Supabase schema live | Mar 2026 | All Phase 1 tables |
| Clerk auth + Supabase Third-Party Auth | Mar 2026 | RS256 JWT, JWKS endpoint |
| `bootstrap_new_user` RPC | Mar 2026 | Atomic onboarding |
| Real-time sync | Mar 2026 | `list_items`, `household_members` |
| 38 seed catalog items | Mar 2026 | |
| Household invite flow | Apr 2026 | 6-char code, 7-day TTL |
| Categories + budget | May 2026 | |
| Staple toggle (⭐) | May 2026 | `is_staple` on catalog_items, swipe-reveal |
| Manage Categories | May 2026 | |
| Per-category "+ Add item" | May 2026 | |
| Real-price-only on My List | May 2026 | |
| "Show prices & budget" toggle | May 2026 | localStorage, defaults hidden |
| Category-average fallback pricing | May 2026 | `~` prefix on estimates |
| `refreshCatalog()` pattern | May 2026 | Replaces `window.location.reload()` |
| Contributor badges `[D][H][E]` | Jun 1, 2026 | `list_item_contributors` table |
| Social share link preview | Jun 5, 2026 | Open Graph + og-image.png |
| **Provision cycles + wrap-up flow** | Jun 6, 2026 | `provision_cycles`, `shopping_sessions`, `known_stores`, roll-forward modal, archive RPC |
| `get_list_items_for_household` JOIN refactor | Jun 8, 2026 | name/category/is_staple inline; dropped separate name-resolver round-trip |
| SHOP list renders from RPC rows (`listRows`) | Jun 8, 2026 | catalogMap out of the display path |
| Per-user-hide-leak fix* | Jun 8, 2026 | Hides no longer suppress shared list rows. *On `dev` — promote once cold-test + merge (NOW #1) completes. |
| Session Scribe moved to git (`CLAUDE.md`) | Jun 8, 2026 | Rolling log in `docs/`, committed to repo; off the Drive connector |
| **Hide verb implemented** | Jun 9, 2026 | `hideItem` wired to SwipeToRemove (all three usages); UI renamed "Remove" → "Hide", recolored to taupe; Add Item restore copy updated with count; poll re-add bug fixed via `hiddenIdsRef` guard in `loadListItems`; boot stacked-poll race fixed via `getTokenRef` |
| Strip debug console.logs | Jun 11, 2026 | 6 `[DEBUG …]` / `[loadListItems]` / `[Poll]` / `[Sync]` logs removed from `useProvisions.js` before main merge |
| **Delete verb** (complete) | Jun 11–12, 2026 | Client: `deleteItem` → `delete_custom_catalog_item` RPC; `is_global` guard; optimistic removal + rollback; Delete button in Edit modal; `isCustom` fix; `deletedIdsRef` poll guard. Server: `delete_custom_catalog_item` SECURITY DEFINER RPC deployed (hard-delete + cascade). |
| **Dev DB sandbox** | Jun 12, 2026 | Second free Supabase project (`zxwtxjjmssykhqrghouf`); Vercel Preview repointed; dev schema rebuilt from `001`–`006` + drift patches; Clerk auth configured. |
| **Catalog propagation — 20s cross-client poll** | Jun 12, 2026 | `refreshCatalog` rewritten as guarded merge (respects `hiddenIdsRef`/`deletedIdsRef`); `refreshCatalogRef` for stable closure access; 20s `catalogPollInterval` wired alongside 2s list poll. Two-client verified (add + delete, both directions, no flicker/clobber). |
| **Browse tab UI overhaul** | Jun 12, 2026 | Real-time search bar; wrapping category chip filters; two-layer `displayCategories` (staples cross-cut → chips narrow); no-match row with category picker + inline new-category creation. `CUSTOM_CAT` constant removed. |
| **dev → main merge, prod deploy** | Jun 12, 2026 | Fast-forward merge; production green at `9a3008d`. Hide/Delete/propagation features live for real users. |
| **Canonical schema baseline** | Jun 12, 2026 | `migrations/000_canonical_baseline.sql` — single file rebuilding prod from empty: 14 objects, 17 canonical functions, 35 RLS policies, 38-item seed. Validated against clean dev rebuild. Six historical files archived in `migrations/archive/`. |
| **SHOP swipe redesign + toggleChecked id fix** | Jun 16, 2026 | SHOP swipe now calls `removeFromList` (list-layer soft-delete of `list_item`) instead of `hideItem` (catalog-layer). Own item: instant remove. Shared item: confirm modal naming the adder. `toggleChecked` rewired to resolve by `catalog_item_id` from `listRows`, eliminating "not in catalog" failures on rolled-forward items. Cold-tested on dev with two members. |
| **Dev grant restoration** | Jun 16, 2026 | Restored `authenticated`/`anon` grants on dev sandbox after "Reset Public Schema Permissions" query stripped them. Wrote `007_dev_restore_role_grants.sql` (dev-only). Root cause of the Jun-13 "permission denied for table households" dev block — was grants, not the `auth.uid()` RLS bug assumed. |
| **Multi-household invite-join flow** | Jun 19, 2026 | New-user auto-switch + established-user silent-join verified end-to-end. Three bugs fixed: invite code survives Clerk sign-up redirect (`index.js` sessionStorage capture before ClerkProvider), Effect 2 resolver trusts `activeHouseholdId` unconditionally (no data split on silent join), join-banner calls `refreshHouseholds()` on any confirmed join (switcher updates without reload). |
| **Concurrent-add 409 + Lemons 409 fixed (migration 008)** | Jun 20, 2026 | `insert_list_item` converted to upsert (INSERT ... ON CONFLICT (household_id, catalog_item_id) DO UPDATE). Concurrent new-item add: second writer folds into update instead of 409ing. Soft-deleted slot resurrection: conflict path clears `deleted_at`, forces `status='pending'`. Both two-window tests passed on dev and prod. |
| **Connectivity pill — soft offline/retry UX** | Jun 20, 2026 | `classifyFetchError` classifier + `ConnectivityContext` state machine + `ConnectivityPill` component. Read paths keep last-good data on transient fail; write paths roll back unconditionally + branch (pill vs red toast); `reportSuccess()` clears pill on any success. Converted: catalog-refresh ×2, list-load, household-fetch, updateQty, toggleChecked. Verified with DevTools network throttling on dev. |
| **Poll-clobber / offline race hardening** | Jun 22, 2026 | Three races fixed: suspect-empty poll guard (empty RPC response bails before setters); transient-vs-genuine rollback classification (offline taps preserve optimistic value; genuine errors roll back); `pendingQtyRef` write guard (in-flight items excluded from 2s poll commits, eliminating 5→4→5 flicker). |
| **Badge-reset RPC — migration 009** | Jun 22, 2026 | `remove_list_item` atomic RPC soft-deletes `list_items` row AND clears `list_item_contributors` in one transaction; fixes badge-resurrection on re-add. Client swapped from plain `.update({deleted_at})`. Dev + prod. |
| **Member management — leave/remove/rejoin (migrations 010–012)** | Jun 22, 2026 | UI: "Created by" attribution, remove controls on non-creator rows, Leave/Delete branching. `remove_member` + `leave_household` (010); `join_household` revive-or-insert (011); `bootstrap_new_user` revive fix (012). Both join paths covered. `refreshMembers` for live actor update. Dev + prod. |
| **Layer 2: removal notice + auto-provision** | Jun 25, 2026 | 30s membership-presence check; typed `systemMessage` channel; variant-B removal notice; auto-provision (`create_household`) on only-household removal; `provisioningRef` in-flight guard. Points 1–4 validated on clean dev env (point 4: DB-gated fixture, `activeHouseholdNameRef` resolved real household name, in-flight guard confirmed — single provisioned household). Bug 1 ("that household" wording) closed as test-environment artifact. Verified on prod: real-name notice + fresh household auto-provisioned, in-flight guard held. |
| **Hide DELETE HOUSEHOLD button + dev→main merge** | Jun 25, 2026 | Owner branch of household-manage ternary renders null (no household-destruction control visible to owners). Removed dead `handleDeleteHousehold` handler + `[ActiveHousehold TEST]` log. 9 commits (8 Layer 2 + cleanup) merged to main, Vercel deploy green. |
| **Delete-household — migration 013 to prod + dev→main merge** | Jun 28, 2026 | Migration 013 (`provision_cycles.deleted_at`, `list_item_contributors.deleted_at`, `delete_household` RPC) applied and verified on prod (body_len 2550). Two-stage owner confirm, D7 custom-item count warning, `resolveAfterHouseholdLoss` shared Layer-2 path. Smoke-tested (DH owner + DT member, orphan count 0). Merged to main; Vercel green. |
| **Member/household-flow defect paydown — six fixes to prod** | Jun 29, 2026 | (1) Member display name reads Supabase `full_name` first at 3 sites (`ec4d4af`); (2) manage-sheet refresh-on-open (`270377e`); (3) name-change hang fixed — `fullName` removed from Effect 1 deps (`4a27ada`); (4) invite-paste auto-switch gated on `joinedId` not `hadPrior` (`75c1481`); (5) join-banner auto-dismiss (5s timer + switch-away clear, `bannerSeenRef` arrival guard) (`0c24e5b`); (6) stale invite-link reset on household change (`90c4316`). Promoted dev→main as clean fast-forward (`eefaa72`→`90c4316`); prod Ready/green, dashboard-verified. No DB changes. |
| **Catalog consistency — shared `CatalogItemRow` + two bug fixes** | Jun 29, 2026 | Extracted a shared `CatalogItemRow` (Browse row inner body) rendered by both Browse and search (`2163929`). Bug 1: search results use the full −/qty/+ stepper instead of a +1-only button, search list wrapped in `.items-grid` (`8f1e471`). Bug 2: Edit Item respects `showPrices` — price field + catalog note gated; new `canEdit` prop on `SwipeToRemove` hides only the Edit button (Staple/Hide kept) for catalog items with pricing off; `openEditModal` early-return guard (`378efec`). `/code-review` high-effort: 0 correctness findings. Fast-forward `90c4316`→`378efec`; Vercel green. See `docs/SPEC_search_row_and_price_gate.md`. No DB changes. |
| **Fix `auth.uid()` RLS bug — migration 014** | Jun 29, 2026 | Rewrote 8 RLS policies across `known_stores`, `shopping_sessions`, `velayo_crew_members`, `velayo_crews` that compared `auth.uid()` (Clerk text `sub`) against uuid columns — always false. Household-membership checks → `is_member_of(household_id)`; ownership (`sessions_insert_own`/`sessions_update_own`) deliberately kept `get_current_user_id()` to preserve owner-is-you (membership alone would let any member touch another's session); crew policies use `get_current_user_id()` (crew-keyed, no `household_id`). RLS enabled/disabled state UNCHANGED (definitions-only). Applied + verified by hand on dev + prod: `auth.uid()` check = 0 rows, A5-style presence ok on all 8. See `docs/SPEC_fix_authuid_rls.md`; harness check A5 added. |
| **Consolidate duplicate helper functions — migration 015** | Jun 29, 2026 | Dropped the two `proconfig=NULL` helper variants (`get_household_id_for_current_user`, `get_user_id_from_clerk`); survivors `get_current_household_id` / `get_current_user_id` pin `search_path = public, extensions` and are deterministic. Zero callers verified (functions + policies, both envs) before drop. Applied + verified on dev + prod: verify query returned exactly 2 survivors. See `docs/SPEC_consolidate_helpers.md`. |
| **Swipe arc — search parity + close-gesture + pointerEvents unblock** | Jun 29, 2026 | Three commits, each built via the new BUILD command (one scoped commit, grep-before-edit, deployed-dev test, stop at dev) then merged dev→main. `41f4952`: wrap search-site `CatalogItemRow` in `SwipeToRemove` so search rows expose the same Hide/Edit/Staple as Browse (one-shared-row principle). `212dfed`: direction-aware `handleEnd` — open catalog rows close on swipe-right past the 60px `SWIPE_THRESHOLD`; sub-threshold drags return to prior state. `22a811d`: content layer `pointerEvents` made unconditional `auto` (was `none` when latched open, which blocked the close gesture from starting; action-button taps unaffected — panel is absolutely-positioned beneath the translated content). All branches converged at `22a811d`; prod smoke-tested clean. See `docs/SPEC_swipe_search_parity.md`, `docs/SPEC_swipe_close_gesture.md`, `docs/SPEC_swipe_close_pointerevents_fix.md`. No DB changes. |
| **`BUILD` workflow command** | Jun 29, 2026 | Added `BUILD` to `CLAUDE.md` (`a195319`): Claude Code implements one airlock spec as one scoped commit — locate spec, read fully, grep-before-edit, implement, test on deployed dev, stop at dev; does NOT route/log/clear (SESSION END's job). Pairs with chat-side `SPEC`. Used 3× this session. [Velayo OS] workflow tooling. |
| **Test House 1–6 cleanup (dev + prod)** | Jun 30, 2026 | Deleted the six clutter test households through the app's own delete-household feature (migration 013 soft-delete) on **both** dev and prod — a real end-to-end exercise of the `delete_household` RPC through the prod UI, not just dev. Switcher cleared cleanly on both; rows remain in `households` with `deleted_at` set (soft-deleted, the intended posture). Removes the false-test-signal risk before the next multi-account session. |
| **List text size (device-local preference)** — *prod-verified (promoted; live)* | Jul 1, 2026 | 5-step stepper (Compact/Default/Large/XL/XXL → 0.9/1.0/1.2/1.45/1.75) in the profile-sheet Preferences; persists the **index** under `localStorage.op_list_text_size` (default 1), never account-synced. Effect drives `--op-list-scale` on `documentElement`; `:root` default prevents first-paint flash. Six row-content classes scale via `calc(<existing> * var(--op-list-scale))`; `flex-wrap:wrap` on `.list-item`+`.item-top` for XL/XXL; chrome unscaled. Built as one scoped commit `b8368f5` via BUILD; `CI=true` clean; verified on dev incl. hard-reload persistence. Live-preview row deferred. See `docs/SPEC_list_text_size.md`. Client-only. **On `dev` — promote on next main merge.** |
| **Join activates the joined household** — *SUPERSEDED by the 2026-07-07 reopen (prod-verified); see Beat 0 DONE* | Jul 1, 2026 | Diagnosis-first (4-hypothesis truth table). H1 ruled out on deployed-dev evidence — `bootstrap_new_user` DOES return `joined_via_invite=true` for an existing user's valid-unconsumed invite (no RPC edit). Root cause = one-shot fragility: the auto-switch lived only on the single invite-consuming load (flags stripped immediately, invite single-use), surviving reload only via the `localStorage.activeHouseholdId` write. Fix (`1c5a916`): replaced the inline one-shot switch in `App.js` with a reactive `pendingJoinId` state + `[pendingJoinId, myHouseholds]` effect that fires `switchHousehold` once the joined household resolves in the membership list, then clears intent — decoupled from the stripped flags and localStorage timing. Lens stays the single writer (`switchHousehold`, never `setHousehold`); dead `acceptInvite` untouched. Verified green on the multi-household profile across three fresh joins (switch + reload-persist); interrupted-window recovery reasoned-correct but not directly reproduced. See `docs/SPEC_join_activates_household.md`. Client-only. **On `dev` — promote on next main merge.** |
| **`full_name` reconciliation + profile-heading fix** | Jul 1, 2026 | Root-caused wrong/missing attribution names to `users.full_name` being NULL for ALL real users — it was only ever written once at bootstrap (before Clerk's name is ready) and bootstrap can't re-run. Fix: **Effect 1b** in `useProvisions.js` (`2b223ff`) — a dedicated, idempotent name-reconciliation effect (decoupled from bootstrap) that writes Clerk `fullName` → `users.full_name` each session when it differs; `lastSyncedNameRef` guards the write loop, reads-before-writes. Reuses the RLS-proven `updateFullName` path; never calls `bootstrap_new_user` (avoids its 4-overload ambiguity). Plus profile heading (`App.js` ~2639, `15712ba`) composes Clerk `firstName`+`lastName` before email fallback (fixes email-over-email). Verified on dev (fresh + existing named users self-populate; nameless degrades cleanly, no write loop); merged to main as clean fast-forwards. Existing NULLs self-heal on next load — no backfill. See `docs/SPEC_full_name_sync.md`. No DB changes. |
| **Phase I active-household indicator** | Jun 28, 2026 | `double+` nameplate treatment — two quiet 10px anchors (50% opacity) flanking household name, right one mirrored via `scaleX(-1)`, `marginInline: 3px`. Absolutely-centered overlay in outer chrome banner (position:absolute so avatar/kebab asymmetry doesn't affect centering). Tap opens manage-household modal. Two-person glyph and "Tap to manage household" subline retired. Shipped to prod, verified live. |
| **Wrap-up modal invisible-card fix** | Jul 3, 2026 | Phantom `className="modal-box"` (never defined in stylesheet) → `.modal` (real card class); `maxWidth: 360px` override preserved. Cancel button and subtext were invisible against the dimmed overlay. One-line fix, commit `91b531a`, promoted to prod via dev→main merge. |

---

## Strategic Bets

- **The Harbor is the moat.** Switching away from OurProvisions is one thing. Leaving the Harbor means losing shared family history across every app. That's a much harder thing to walk away from.
- **Prices are infrastructure.** The receipt scanner is the intelligence layer. Everything — cross-store comparison, waste detection, savings nudges — flows from it.
- **Digital receipts as distribution.** Grocery chains pivoting to digital receipts represent a partnership and scaling opportunity, not just a data source.
- **Capture signals silently.** Check-off sequence, GPS, timing — captured in the background. No user configuration needed.
- **Provision cycles not just sessions.** The planning unit is a cycle (Plan → Shop → Review → repeat). Sessions are how it gets fulfilled. This model handles partial shops, multi-person splits, and impromptu runs cleanly.
- **The shared list is sacred.** No per-user view preference (hide, filter) ever suppresses what the household has collectively decided to buy. Personalization lives in the view layer; the list is shared truth. *(Added June 8 — the principle behind the per-user-hide fix.)*

---

*Velayo, Inc. — velayo.ai — dan@velayo.ai*
